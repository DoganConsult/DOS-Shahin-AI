{
  "name": "@dos/vendor-risk-os-service",
  "version": "0.1.0",
  "description": "DOS Master Phase 2 — Vendor risk record",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dos/service-bootstrap": "workspace:*",
    "@dos/runtime-config": "workspace:*",
    "@dos/db": "workspace:*",
    "express": "^4.22.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0"
  }
}
