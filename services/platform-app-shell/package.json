{
  "name": "@dos/platform-app-shell",
  "version": "0.1.0",
  "description": "Dogan-AI OS Platform App shell — serves platform/app Angular SPA on :3010 and proxies /api + /events to gateway.",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dos/platform-core": "workspace:*",
    "express": "^4.22.0",
    "compression": "^1.7.4",
    "helmet": "^8.0.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/compression": "^1.7.5",
    "@types/cors": "^2.8.17",
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0"
  }
}
