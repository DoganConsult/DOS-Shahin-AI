import { z } from 'zod';

export const updateActionItemBody = z.object({});
export const bulkCreateBody = z.object({});
export const bulkDeleteBody = z.object({});
export const listQuerySchema = z.object({});
export const createActionItemBody = z.object({});
