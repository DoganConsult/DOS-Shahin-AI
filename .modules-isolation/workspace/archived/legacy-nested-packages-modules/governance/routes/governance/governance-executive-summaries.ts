import type { Router as ExpressRouter } from 'express';
import { Router } from 'express';
const governanceExecutiveSummariesRoutes: ExpressRouter = Router();
export default governanceExecutiveSummariesRoutes;
