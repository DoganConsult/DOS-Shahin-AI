import { CONFIG_CENTER_MANIFEST } from '../config-center.module';

export const CONFIG_CENTER_PERMISSIONS = CONFIG_CENTER_MANIFEST.securityPermissions;
export const CONFIG_CENTER_ROLES = CONFIG_CENTER_MANIFEST.securityRoles;
export const CONFIG_CENTER_ACTIONS = CONFIG_CENTER_MANIFEST.securityActions;
