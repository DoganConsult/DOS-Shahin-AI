import type { RegistrationDefaultsContract } from '@dos/contracts';

export const SHAHIN_REGISTRATION_DEFAULTS: RegistrationDefaultsContract = {
  ownerRole: 'tenant_admin',
  userType: 'internal',
  initialStatus: 'active',
};
