-- dos:extracted-from: ops/migrations/tenant/118_incident_vendor_bcp_config_tables.sql
-- dos:supersedes-checksum: 5409221c280b107c952fccc8259f964edc47f4ed57c77b3b0a377f7e8a5fc171
-- dos:owning-module: vendor
-- dos:tables: vendor_due_diligence_checklists, vendor_questionnaire_templates, vendor_tier_config
-- dos:statement-count: 3
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
-- ═══ VENDOR MODULE CONFIG ═══

CREATE TABLE IF NOT EXISTS vendor_tier_config (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tier_code VARCHAR(20) NOT NULL UNIQUE,
  tier_label VARCHAR(50) NOT NULL,
  tier_level INT NOT NULL,
  review_frequency_days INT DEFAULT 365,
  due_diligence_required BOOLEAN DEFAULT TRUE,
  questionnaire_required BOOLEAN DEFAULT TRUE,
  sla_assessment_days INT DEFAULT 30,
  risk_threshold_score INT DEFAULT 50,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS vendor_questionnaire_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  template_code VARCHAR(50) NOT NULL UNIQUE,
  template_name VARCHAR(200) NOT NULL,
  description TEXT,
  category VARCHAR(50) DEFAULT 'general',
  questions JSONB DEFAULT '[]',
  scoring_model JSONB DEFAULT '{}',
  tier_codes TEXT[] DEFAULT '{}',
  is_active BOOLEAN DEFAULT TRUE,
  created_by VARCHAR(64) DEFAULT 'system',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS vendor_due_diligence_checklists (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  checklist_code VARCHAR(50) NOT NULL UNIQUE,
  checklist_name VARCHAR(200) NOT NULL,
  items JSONB DEFAULT '[]',
  tier_codes TEXT[] DEFAULT '{}',
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
