/**
 * vendor — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const VENDOR_MODULE_STATES = [
  'identified', 'questionnaire_sent', 'questionnaire_received', 'assessing',
  'assessed', 'approved', 'rejected', 'onboarded', 'active',
  'annual_review', 're_assessed', 'offboarding', 'offboarded',
] as const;

const VENDOR_MODULE_TRANSITIONS: Record<string, string[]> = {
  identified:              ['questionnaire_sent'],
  questionnaire_sent:      ['questionnaire_received'],
  questionnaire_received:  ['assessing'],
  assessing:               ['assessed'],
  assessed:                ['approved', 'rejected'],
  approved:                ['onboarded'],
  rejected:                [],
  onboarded:               ['active'],
  active:                  ['annual_review', 'offboarding'],
  annual_review:           ['re_assessed'],
  re_assessed:             ['active', 'offboarding'],
  offboarding:             ['offboarded'],
  offboarded:              [],
};

registerLifecycleDefinition(
  'vendor',
  'vendor',
  VENDOR_MODULE_STATES,
  VENDOR_MODULE_TRANSITIONS,
  {
    initialState: 'identified',
    terminalStates: ['offboarded', 'rejected'],
    transitionPermissions: {
      'identified->questionnaire_sent': 'vendor.record.create',
      'questionnaire_sent->questionnaire_received': 'vendor.record.create',
      'questionnaire_received->assessing': 'vendor.assessment.execute',
      'assessing->assessed': 'vendor.assessment.execute',
      'assessed->approved': 'vendor.assessment.approve',
      'assessed->rejected': 'vendor.assessment.approve',
      'approved->onboarded': 'vendor.record.create',
      'onboarded->active': 'vendor.record.create',
      'active->annual_review': 'vendor.assessment.execute',
      'annual_review->re_assessed': 'vendor.assessment.execute',
      'active->offboarding': 'vendor.record.create',
      'offboarding->offboarded': 'vendor.record.create',
    },
  },
);

const VENDOR_ENGAGEMENTS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const VENDOR_ENGAGEMENTS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'vendor',
  'vendor_engagements',
  VENDOR_ENGAGEMENTS_STATES,
  VENDOR_ENGAGEMENTS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const VENDOR_RISK_ASSESSMENTS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const VENDOR_RISK_ASSESSMENTS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'vendor',
  'vendor_risk_assessments',
  VENDOR_RISK_ASSESSMENTS_STATES,
  VENDOR_RISK_ASSESSMENTS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const VENDOR_DUE_DILIGENCE_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const VENDOR_DUE_DILIGENCE_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'vendor',
  'vendor_due_diligence',
  VENDOR_DUE_DILIGENCE_STATES,
  VENDOR_DUE_DILIGENCE_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const VENDOR_SLA_DEFINITIONS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const VENDOR_SLA_DEFINITIONS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'vendor',
  'vendor_sla_definitions',
  VENDOR_SLA_DEFINITIONS_STATES,
  VENDOR_SLA_DEFINITIONS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
