/**
 * incident — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const INCIDENT_MODULE_STATES = [
  'reported', 'triaged', 'investigating', 'contained', 'eradicated',
  'recovered', 'under_review', 'approved_closure', 'closed',
  'lessons_learned', 'escalated',
] as const;

const INCIDENT_MODULE_TRANSITIONS: Record<string, string[]> = {
  reported:         ['triaged'],
  triaged:          ['investigating'],
  investigating:    ['contained'],
  contained:        ['escalated', 'eradicated'],
  eradicated:       ['recovered'],
  recovered:        ['under_review'],
  under_review:     ['approved_closure'],
  approved_closure: ['closed'],
  closed:           ['lessons_learned'],
  lessons_learned:  [],
  escalated:        ['eradicated'],
};

registerLifecycleDefinition(
  'incident',
  'incident',
  INCIDENT_MODULE_STATES,
  INCIDENT_MODULE_TRANSITIONS,
  {
    initialState: 'reported',
    terminalStates: ['closed', 'lessons_learned'],
    transitionPermissions: {
      'reported->triaged': 'incident.record.update',
      'triaged->investigating': 'incident.record.update',
      'investigating->contained': 'incident.record.update',
      'contained->escalated': 'incident.record.escalate',
      'contained->eradicated': 'incident.record.update',
      'eradicated->recovered': 'incident.record.update',
      'recovered->under_review': 'incident.record.review',
      'under_review->approved_closure': 'incident.record.approve',
      'approved_closure->closed': 'incident.record.approve',
      'closed->lessons_learned': 'incident.record.review',
    },
  },
);

const INCIDENT_RESPONSE_PLANS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const INCIDENT_RESPONSE_PLANS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'incident',
  'incident_response_plans',
  INCIDENT_RESPONSE_PLANS_STATES,
  INCIDENT_RESPONSE_PLANS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const INCIDENT_CATEGORIES_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const INCIDENT_CATEGORIES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'incident',
  'incident_categories',
  INCIDENT_CATEGORIES_STATES,
  INCIDENT_CATEGORIES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const INCIDENT_SEVERITY_MATRIX_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const INCIDENT_SEVERITY_MATRIX_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'incident',
  'incident_severity_matrix',
  INCIDENT_SEVERITY_MATRIX_STATES,
  INCIDENT_SEVERITY_MATRIX_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
