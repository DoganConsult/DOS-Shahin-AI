-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=incident scope=public function=core
-- ledger: dos.schema_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- modules/incident/db/public/migrations/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (2):
--   * above
--   * incidents
--
-- Source migrations (immutable, 1):
--   * modules/incident/db/public/migrations/001_extracted_from_001_risk_incident_tables.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS dos.incidents (
  incident_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id VARCHAR(16) NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  severity VARCHAR(20) NOT NULL DEFAULT 'medium',
  status VARCHAR(50) NOT NULL DEFAULT 'open',
  risk_id UUID REFERENCES dos.risks(risk_id),
  reported_by VARCHAR(64),
  assigned_to VARCHAR(64),
  resolved_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ,
  is_deleted BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE TABLE IF NOT EXISTS above no-ops
-- when the legacy shape already exists, so we idempotently add the missing
-- column + FK here before the index.
ALTER TABLE dos.incidents ADD COLUMN IF NOT EXISTS risk_id UUID;

CREATE INDEX IF NOT EXISTS idx_incidents_tenant ON dos.incidents (tenant_id);

CREATE INDEX IF NOT EXISTS idx_incidents_risk ON dos.incidents (risk_id);
