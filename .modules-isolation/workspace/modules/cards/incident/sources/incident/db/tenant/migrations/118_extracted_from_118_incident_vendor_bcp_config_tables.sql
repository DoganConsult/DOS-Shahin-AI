-- dos:extracted-from: ops/migrations/tenant/118_incident_vendor_bcp_config_tables.sql
-- dos:supersedes-checksum: 5409221c280b107c952fccc8259f964edc47f4ed57c77b3b0a377f7e8a5fc171
-- dos:owning-module: incident
-- dos:tables: incident_categories, incident_notification_templates, incident_response_teams, incident_severity_matrix
-- dos:statement-count: 4
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
-- ============================================================================
-- 118_incident_vendor_bcp_config_tables.sql
-- Ported from monolith: backend/src/migrations/tenant/762_incident_vendor_bcp_config_tables.sql
-- Runs in per-tenant schema. run-tenant-migrations.sh sets search_path.
-- ============================================================================
-- Migration 762: Config/baseline tables for Class B modules (incident, vendor, bcp, asset)

-- ═══ INCIDENT MODULE CONFIG ═══

CREATE TABLE IF NOT EXISTS incident_severity_matrix (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  severity_code VARCHAR(20) NOT NULL UNIQUE,
  severity_label VARCHAR(50) NOT NULL,
  severity_level INT NOT NULL,
  sla_response_hours INT NOT NULL DEFAULT 4,
  sla_resolution_hours INT NOT NULL DEFAULT 24,
  escalation_role VARCHAR(50) DEFAULT 'admin',
  notification_template VARCHAR(50),
  color VARCHAR(7) DEFAULT '#ff0000',
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.incident_categories (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  category_code VARCHAR(50) NOT NULL UNIQUE,
  category_label VARCHAR(100) NOT NULL,
  parent_category_id UUID,
  default_severity VARCHAR(20),
  playbook_id UUID,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS incident_response_teams (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  team_name VARCHAR(100) NOT NULL,
  team_type VARCHAR(30) DEFAULT 'primary' CHECK (team_type IN ('primary','escalation','executive','external')),
  members JSONB DEFAULT '[]',
  on_call_schedule JSONB DEFAULT '{}',
  notification_channels TEXT[] DEFAULT '{}',
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS incident_notification_templates (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  template_code VARCHAR(50) NOT NULL UNIQUE,
  template_name VARCHAR(100) NOT NULL,
  subject_template TEXT,
  body_template TEXT,
  channels TEXT[] DEFAULT '{email}',
  trigger_event VARCHAR(50),
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);
