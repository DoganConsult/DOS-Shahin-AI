/**
 * audit — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const AUDIT_MODULE_STATES = [
  'planned', 'scoped', 'fieldwork', 'draft_report', 'management_response',
  'final_report', 'approved', 'issued', 'finding_tracking', 'closed',
] as const;

const AUDIT_MODULE_TRANSITIONS: Record<string, string[]> = {
  planned:             ['scoped'],
  scoped:              ['fieldwork'],
  fieldwork:           ['draft_report'],
  draft_report:        ['management_response'],
  management_response: ['final_report'],
  final_report:        ['approved'],
  approved:            ['issued'],
  issued:              ['finding_tracking'],
  finding_tracking:    ['closed'],
  closed:              [],
};

registerLifecycleDefinition(
  'audit',
  'audit',
  AUDIT_MODULE_STATES,
  AUDIT_MODULE_TRANSITIONS,
  {
    initialState: 'planned',
    terminalStates: ['closed'],
    transitionPermissions: {
      'planned->scoped': 'audit.engagement.create',
      'scoped->fieldwork': 'audit.workpaper.update',
      'fieldwork->draft_report': 'audit.report.create',
      'draft_report->management_response': 'audit.finding.respond',
      'management_response->final_report': 'audit.report.create',
      'final_report->approved': 'audit.report.approve',
      'approved->issued': 'audit.report.approve',
      'issued->finding_tracking': 'audit.finding.issue',
      'finding_tracking->closed': 'audit.finding.close',
    },
  },
);

const AUDITS_STATES = [
  'planning',
  'fieldwork',
  'reporting',
  'review',
  'issued',
  'closed',
  'archived',
] as const;

const AUDITS_TRANSITIONS: Record<string, string[]> = {
  planning        : ['fieldwork'],
  fieldwork       : ['reporting'],
  reporting       : ['review'],
  review          : ['issued', 'reporting'],
  issued          : ['closed'],
  closed          : ['archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'audit',
  'audits',
  AUDITS_STATES,
  AUDITS_TRANSITIONS,
  {
    initialState: 'planning',
    terminalStates: ['archived'],
  },
);

const AUDIT_UNIVERSE_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const AUDIT_UNIVERSE_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'audit',
  'audit_universe',
  AUDIT_UNIVERSE_STATES,
  AUDIT_UNIVERSE_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const AUDIT_SCHEDULES_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const AUDIT_SCHEDULES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'audit',
  'audit_schedules',
  AUDIT_SCHEDULES_STATES,
  AUDIT_SCHEDULES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const AUDIT_WORKING_PAPERS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const AUDIT_WORKING_PAPERS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'audit',
  'audit_working_papers',
  AUDIT_WORKING_PAPERS_STATES,
  AUDIT_WORKING_PAPERS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
