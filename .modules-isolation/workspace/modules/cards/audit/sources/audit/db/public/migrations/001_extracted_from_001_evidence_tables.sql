-- dos:extracted-from: services/evidence-audit-reporting-service/migrations/001_evidence_tables.sql
-- dos:supersedes-checksum: 0be63305bcf6e7463fb808266dc15b60daee400cf48a30685688bd534723f034
-- dos:owning-module: audit
-- dos:tables: audit_findings
-- dos:statement-count: 2
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
CREATE TABLE IF NOT EXISTS dos.audit_findings (
  finding_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id VARCHAR(16) NOT NULL,
  title TEXT NOT NULL,
  description TEXT,
  severity VARCHAR(20) NOT NULL DEFAULT 'medium',
  status VARCHAR(50) NOT NULL DEFAULT 'open',
  evidence_id UUID REFERENCES dos.evidences(evidence_id),
  assigned_to VARCHAR(64),
  due_date DATE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ,
  is_deleted BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX IF NOT EXISTS idx_findings_tenant ON dos.audit_findings (tenant_id);
