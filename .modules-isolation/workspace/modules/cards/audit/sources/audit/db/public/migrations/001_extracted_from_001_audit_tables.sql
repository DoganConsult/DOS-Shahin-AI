-- dos:extracted-from: services/audit-service/migrations/001_audit_tables.sql
-- dos:supersedes-checksum: c639d9fc6c22a0d0a56170724defa5200b5280f0887fdf7daab542e16834f23a
-- dos:owning-module: audit
-- dos:tables: audit_log_archive
-- dos:statement-count: 1
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
CREATE TABLE IF NOT EXISTS dos.audit_log_archive (
  id            SERIAL PRIMARY KEY,
  tenant_id     VARCHAR(16),
  user_id       VARCHAR(64),
  action        VARCHAR(100),
  module_code   VARCHAR(50),
  entity_type   VARCHAR(50),
  entity_id     VARCHAR(64),
  before_state  JSONB,
  after_state   JSONB,
  ip_address    VARCHAR(45),
  correlation_id VARCHAR(64),
  created_at    TIMESTAMPTZ DEFAULT NOW()
);
