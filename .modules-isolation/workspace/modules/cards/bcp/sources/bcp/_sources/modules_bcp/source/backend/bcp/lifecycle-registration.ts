/**
 * bcp — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const BCP_MODULE_STATES = [
  'draft', 'planned', 'tested', 'evaluated', 'approved', 'active',
  'exercised', 'review_due', 'under_revision', 're_approved', 'retired',
] as const;

const BCP_MODULE_TRANSITIONS: Record<string, string[]> = {
  draft:           ['planned'],
  planned:         ['tested'],
  tested:          ['evaluated'],
  evaluated:       ['approved'],
  approved:        ['active'],
  active:          ['exercised', 'review_due', 'retired'],
  exercised:       ['review_due'],
  review_due:      ['under_revision'],
  under_revision:  ['re_approved'],
  re_approved:     ['active'],
  retired:         [],
};

registerLifecycleDefinition(
  'bcp',
  'bcp',
  BCP_MODULE_STATES,
  BCP_MODULE_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['retired'],
    transitionPermissions: {
      'draft->planned': 'bcp.plan.update',
      'planned->tested': 'bcp.plan.update',
      'tested->evaluated': 'bcp.exercise.approve',
      'evaluated->approved': 'bcp.exercise.approve',
      'approved->active': 'bcp.plan.update',
      'active->exercised': 'bcp.plan.update',
      'exercised->review_due': 'bcp.plan.update',
      'review_due->under_revision': 'bcp.plan.update',
      'under_revision->re_approved': 'bcp.exercise.approve',
      're_approved->active': 'bcp.plan.update',
      'active->retired': 'bcp.plan.update',
    },
  },
);

const BCP_PLANS_STATES = [
  'draft',
  'review',
  'approved',
  'active',
  'testing',
  'expired',
  'archived',
] as const;

const BCP_PLANS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['review'],
  review          : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['testing', 'expired'],
  testing         : ['active'],
  expired         : ['archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'bcp',
  'bcp_plans',
  BCP_PLANS_STATES,
  BCP_PLANS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const BCP_ASSESSMENTS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const BCP_ASSESSMENTS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'bcp',
  'bcp_assessments',
  BCP_ASSESSMENTS_STATES,
  BCP_ASSESSMENTS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const BCP_EXERCISES_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const BCP_EXERCISES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'bcp',
  'bcp_exercises',
  BCP_EXERCISES_STATES,
  BCP_EXERCISES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const BCP_RECOVERY_OBJECTIVES_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const BCP_RECOVERY_OBJECTIVES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'bcp',
  'bcp_recovery_objectives',
  BCP_RECOVERY_OBJECTIVES_STATES,
  BCP_RECOVERY_OBJECTIVES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
