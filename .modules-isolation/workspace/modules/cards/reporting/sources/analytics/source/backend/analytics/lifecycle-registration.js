"use strict";
/**
 * analytics — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */
Object.defineProperty(exports, "__esModule", { value: true });
const lifecycle_port_1 = require("./ports/lifecycle.port");
const ANALYTICS_DASHBOARDS_STATES = [
    'draft',
    'in_review',
    'approved',
    'active',
    'suspended',
    'archived',
];
const ANALYTICS_DASHBOARDS_TRANSITIONS = {
    draft: ['in_review'],
    in_review: ['approved', 'draft'],
    approved: ['active'],
    active: ['suspended', 'archived'],
    suspended: ['active', 'archived'],
    archived: [],
};
(0, lifecycle_port_1.registerLifecycleDefinition)('analytics', 'analytics_dashboards', ANALYTICS_DASHBOARDS_STATES, ANALYTICS_DASHBOARDS_TRANSITIONS, {
    initialState: 'draft',
    terminalStates: ['archived'],
});
const ANALYTICS_DATASETS_STATES = [
    'draft',
    'in_review',
    'approved',
    'active',
    'suspended',
    'archived',
];
const ANALYTICS_DATASETS_TRANSITIONS = {
    draft: ['in_review'],
    in_review: ['approved', 'draft'],
    approved: ['active'],
    active: ['suspended', 'archived'],
    suspended: ['active', 'archived'],
    archived: [],
};
(0, lifecycle_port_1.registerLifecycleDefinition)('analytics', 'analytics_datasets', ANALYTICS_DATASETS_STATES, ANALYTICS_DATASETS_TRANSITIONS, {
    initialState: 'draft',
    terminalStates: ['archived'],
});
const ANALYTICS_METRICS_STATES = [
    'draft',
    'in_review',
    'approved',
    'active',
    'suspended',
    'archived',
];
const ANALYTICS_METRICS_TRANSITIONS = {
    draft: ['in_review'],
    in_review: ['approved', 'draft'],
    approved: ['active'],
    active: ['suspended', 'archived'],
    suspended: ['active', 'archived'],
    archived: [],
};
(0, lifecycle_port_1.registerLifecycleDefinition)('analytics', 'analytics_metrics', ANALYTICS_METRICS_STATES, ANALYTICS_METRICS_TRANSITIONS, {
    initialState: 'draft',
    terminalStates: ['archived'],
});
// ── ClickHouse Analytics Table Initialization ────────────────────────────────
// Ensures audit_events, agent_metrics, api_request_logs tables exist on startup.
// Non-fatal: analytics continues with PostgreSQL-only if ClickHouse is unavailable.
const clickhouse_client_1 = require("../../../../config/clickhouse-client");
if ((0, clickhouse_client_1.isClickHouseEnabled)()) {
    (0, clickhouse_client_1.ensureClickHouseTables)().catch((err) => console.warn('[analytics] ClickHouse table init failed (non-fatal):', err instanceof Error ? err.message : String(err)));
}
//# sourceMappingURL=lifecycle-registration.js.map