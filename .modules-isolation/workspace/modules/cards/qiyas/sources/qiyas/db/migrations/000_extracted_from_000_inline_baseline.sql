-- dos:extracted-from: ops/migrations/tenant/000_inline_baseline.sql
-- dos:supersedes-checksum: 22a6a5a32f0e140282bc2ad55c48409bab16be318bc70a3462722cdb2b5227cb
-- dos:owning-module: qiyas
-- dos:tables: qiyas_benchmark_profiles, qiyas_models, qiyas_rating_scales, qiyas_scoring_methods
-- dos:statement-count: 4
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.qiyas_models (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT NOT NULL,
  name_en TEXT,
  name_ar TEXT,
  description_en TEXT,
  model_type TEXT DEFAULT 'maturity',
  status TEXT DEFAULT 'active',
  tenant_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(code)
);

CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.qiyas_benchmark_profiles (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT NOT NULL,
  name_en TEXT,
  name_ar TEXT,
  benchmark_type TEXT,
  config JSONB DEFAULT '{}',
  tenant_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(code)
);

CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.qiyas_rating_scales (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT NOT NULL,
  name_en TEXT,
  name_ar TEXT,
  scale_type TEXT,
  levels JSONB DEFAULT '[]',
  min_value NUMERIC DEFAULT 0,
  max_value NUMERIC DEFAULT 5,
  tenant_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(code)
);

CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.qiyas_scoring_methods (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  code TEXT NOT NULL,
  name_en TEXT,
  name_ar TEXT,
  method_type TEXT,
  tenant_id TEXT,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(code)
);
