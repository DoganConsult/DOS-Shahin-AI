/**
 * Product composition nav source — stub.
 * Nav items resolved from DB via /api/ui-os/module-nav.
 */
import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class ProductCompositionNavSource {
  getNavItems() { return []; }
}
