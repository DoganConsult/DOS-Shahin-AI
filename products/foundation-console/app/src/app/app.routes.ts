// Routes — empty by design. F5: shell mounts at app root via AppComponent.
// Doctrine: NO hardcoded route paths. DB-driven page resolution lands in F6.
import type { Routes } from '@angular/router';

export const APP_ROUTES: Routes = [];
