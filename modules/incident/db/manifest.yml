# Ownership manifest for modules/incident
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: incident
version: 1
migrationsDir:
  tenant: modules/incident/db/tenant/migrations
  public: modules/incident/db/public/migrations
ownedTables:
  tenant:
    prefix: [incident, incidents, near, pir]
    # 37 tenant tables owned (derived from catalog)
    count: 37
  public:
    # 23 public tables owned
    count: 23
  explicit: [vulnerability_findings_map, near_miss_reports, pir_sign_offs, findings]
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 11
