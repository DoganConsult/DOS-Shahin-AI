/**
 * governance — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const GOVERNANCE_MODULE_STATES = [
  'draft', 'proposed', 'committee_review', 'approved', 'active',
  'annual_review', 'under_revision', 're_approved', 'dissolved',
] as const;

const GOVERNANCE_MODULE_TRANSITIONS: Record<string, string[]> = {
  draft:            ['proposed'],
  proposed:         ['committee_review'],
  committee_review: ['approved'],
  approved:         ['active'],
  active:           ['annual_review', 'dissolved'],
  annual_review:    ['under_revision'],
  under_revision:   ['re_approved'],
  re_approved:      ['active'],
  dissolved:        [],
};

registerLifecycleDefinition(
  'governance',
  'governance',
  GOVERNANCE_MODULE_STATES,
  GOVERNANCE_MODULE_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['dissolved'],
    transitionPermissions: {
      'draft->proposed': 'governance.charter.update',
      'proposed->committee_review': 'governance.meeting.manage',
      'committee_review->approved': 'governance.body.create',
      'approved->active': 'governance.charter.update',
      'active->annual_review': 'governance.charter.update',
      'annual_review->under_revision': 'governance.charter.update',
      'under_revision->re_approved': 'governance.body.create',
      're_approved->active': 'governance.charter.update',
      'active->dissolved': 'governance.body.create',
    },
  },
);

const GOVERNANCE_BODIES_STATES = [
  'draft',
  'review',
  'approved',
  'active',
  'superseded',
  'archived',
] as const;

const GOVERNANCE_BODIES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['review'],
  review          : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['superseded', 'archived'],
  superseded      : ['archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'governance',
  'governance_bodies',
  GOVERNANCE_BODIES_STATES,
  GOVERNANCE_BODIES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const GOVERNANCE_CHARTERS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const GOVERNANCE_CHARTERS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'governance',
  'governance_charters',
  GOVERNANCE_CHARTERS_STATES,
  GOVERNANCE_CHARTERS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const GOVERNANCE_BOARD_PACKS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const GOVERNANCE_BOARD_PACKS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'governance',
  'governance_board_packs',
  GOVERNANCE_BOARD_PACKS_STATES,
  GOVERNANCE_BOARD_PACKS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const GOVERNANCE_MANDATES_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const GOVERNANCE_MANDATES_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'governance',
  'governance_mandates',
  GOVERNANCE_MANDATES_STATES,
  GOVERNANCE_MANDATES_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
