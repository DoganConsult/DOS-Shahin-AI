import type { Router as ExpressRouter } from 'express';
import { Router } from 'express';
const scoringPoliciesStandaloneRoutes: ExpressRouter = Router();
export default scoringPoliciesStandaloneRoutes;
