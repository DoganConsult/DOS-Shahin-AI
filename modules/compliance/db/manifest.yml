# Ownership manifest for modules/compliance
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: compliance
version: 1
migrationsDir:
  tenant: modules/compliance/db/tenant/migrations
  public: modules/compliance/db/public/migrations
ownedTables:
  tenant:
    prefix: [compliance, csa, crosswalk, sod]
    # 43 tenant tables owned (derived from catalog)
    count: 43
  public:
    # 8 public tables owned
    count: 8
  explicit: [assessments, maturity_assessments, obligations, scores, retired_entities, entities]
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 9
