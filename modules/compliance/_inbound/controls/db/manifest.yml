# Ownership manifest for modules/controls
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: controls
version: 1
migrationsDir:
  tenant: modules/controls/db/tenant/migrations
  public: modules/controls/db/public/migrations
ownedTables:
  tenant:
    prefix: [controls, control]
    # 40 tenant tables owned (derived from catalog)
    count: 40
  public:
    # 1 public tables owned
    count: 1
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 5
