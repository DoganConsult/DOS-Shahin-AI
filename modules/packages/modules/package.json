{
  "name": "@dos/legacy-modules",
  "version": "0.0.1",
  "private": true,
  "description": "DOS Platform \u2014 legacy module controller shims (compat). Canonical sources live in modules/*.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}