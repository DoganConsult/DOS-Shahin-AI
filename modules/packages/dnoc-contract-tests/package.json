{
  "name": "@dos/dnoc-contract-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Shared contract-test suite for DNOCPort consumers.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@dos/ports": "workspace:*"
  },
  "peerDependencies": {
    "@dos/ports": "^1.0.0",
    "vitest": ">=1.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^4.1.4"
  }
}