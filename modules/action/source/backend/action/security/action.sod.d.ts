/**
 * Action Module — Separation of Duties Rules
 * Prevents conflict-of-interest violations within the action lifecycle.
 * @owner Module:action
 */
import type { SoDRule } from '@dos/types';
export declare const ACTION_SOD_RULES: SoDRule[];
