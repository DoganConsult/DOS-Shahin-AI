-- dos:extracted-from: services/remediation-action-service/migrations/001_remediation_tables.sql
-- dos:supersedes-checksum: 7276a02b53cc6f5f02bae0c54960bf2a02a721ac0059780d1155ebdb2c31477d
-- dos:owning-module: action
-- dos:tables: action_items
-- dos:statement-count: 3
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
CREATE TABLE IF NOT EXISTS dos.action_items (
  action_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id VARCHAR(16) NOT NULL,
  remediation_id UUID REFERENCES dos.remediations(remediation_id),
  title TEXT NOT NULL,
  status VARCHAR(50) NOT NULL DEFAULT 'pending',
  assigned_to VARCHAR(64),
  due_date DATE,
  completed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ,
  is_deleted BOOLEAN NOT NULL DEFAULT FALSE
);

CREATE INDEX IF NOT EXISTS idx_actions_tenant ON dos.action_items (tenant_id);

CREATE INDEX IF NOT EXISTS idx_actions_remediation ON dos.action_items (remediation_id);
