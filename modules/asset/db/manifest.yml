# Ownership manifest for modules/asset
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: asset
version: 1
migrationsDir:
  tenant: modules/asset/db/tenant/migrations
  public: modules/asset/db/public/migrations
ownedTables:
  tenant:
    prefix: [asset, assets]
    # 41 tenant tables owned (derived from catalog)
    count: 41
  public:
    # 3 public tables owned
    count: 3
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 5
