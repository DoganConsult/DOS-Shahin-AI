/**
 * analytics — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const ANALYTICS_DASHBOARDS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const ANALYTICS_DASHBOARDS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'analytics',
  'analytics_dashboards',
  ANALYTICS_DASHBOARDS_STATES,
  ANALYTICS_DASHBOARDS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const ANALYTICS_DATASETS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const ANALYTICS_DATASETS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'analytics',
  'analytics_datasets',
  ANALYTICS_DATASETS_STATES,
  ANALYTICS_DATASETS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const ANALYTICS_METRICS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const ANALYTICS_METRICS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'analytics',
  'analytics_metrics',
  ANALYTICS_METRICS_STATES,
  ANALYTICS_METRICS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

// ── ClickHouse Analytics Table Initialization ────────────────────────────────
// Ensures audit_events, agent_metrics, api_request_logs tables exist on startup.
// Non-fatal: analytics continues with PostgreSQL-only if ClickHouse is unavailable.

import { isClickHouseEnabled, ensureClickHouseTables } from '../../config/clickhouse-client';

if (isClickHouseEnabled()) {
  ensureClickHouseTables().catch((err: unknown) =>
    console.warn('[analytics] ClickHouse table init failed (non-fatal):', err instanceof Error ? err.message : String(err)),
  );
}
