-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=foundation scope=tenant function=workspaces
-- ledger: dos.tenant_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- modules/foundation/db/tenant/migrations/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (14):
--   * business_units
--   * committee_members
--   * committees
--   * departments
--   * location_bu_map
--   * locations
--   * organizations
--   * ownership_mappings
--   * position_assignments
--   * positions
--   * sections
--   * team_members
--   * teams
--   * workspaces
--
-- Source migrations (immutable, 1):
--   * modules/foundation/db/tenant/migrations/001_foundation_tenant_tables.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".workspaces (
  workspace_id   UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name           TEXT NOT NULL DEFAULT 'Default Workspace',
  status         TEXT NOT NULL DEFAULT 'provisioning',
  tenant_id      TEXT,
  config_json    JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".organizations (
  organization_id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name_en         TEXT NOT NULL,
  name_ar         TEXT,
  code            TEXT,
  parent_id       UUID,
  org_type        TEXT,
  status          TEXT NOT NULL DEFAULT 'active',
  description     TEXT,
  metadata_json   JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_by      VARCHAR(64),
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at      TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_orgs_parent      ON "__TENANT_SCHEMA__".organizations(parent_id) WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_orgs_status      ON "__TENANT_SCHEMA__".organizations(status)    WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".business_units (
  bu_id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  organization_id  UUID REFERENCES "__TENANT_SCHEMA__".organizations(organization_id) ON DELETE SET NULL,
  parent_bu_id     UUID,
  name_en          TEXT NOT NULL,
  name_ar          TEXT,
  code             TEXT,
  bu_type          TEXT,
  status           TEXT NOT NULL DEFAULT 'active',
  description      TEXT,
  metadata_json    JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_by       VARCHAR(64),
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at       TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_bu_org    ON "__TENANT_SCHEMA__".business_units(organization_id) WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_bu_parent ON "__TENANT_SCHEMA__".business_units(parent_bu_id)    WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".departments (
  department_id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  bu_id            UUID REFERENCES "__TENANT_SCHEMA__".business_units(bu_id) ON DELETE SET NULL,
  parent_dept_id   UUID,
  name_en          TEXT NOT NULL,
  name_ar          TEXT,
  code             TEXT,
  status           TEXT NOT NULL DEFAULT 'active',
  description      TEXT,
  metadata_json    JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_by       VARCHAR(64),
  created_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at       TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_dept_bu     ON "__TENANT_SCHEMA__".departments(bu_id)          WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_dept_parent ON "__TENANT_SCHEMA__".departments(parent_dept_id) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".sections (
  section_id     UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  department_id  UUID REFERENCES "__TENANT_SCHEMA__".departments(department_id) ON DELETE SET NULL,
  name_en        TEXT NOT NULL,
  name_ar        TEXT,
  code           TEXT,
  status         TEXT NOT NULL DEFAULT 'active',
  description    TEXT,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at     TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_sect_dept ON "__TENANT_SCHEMA__".sections(department_id) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".teams (
  team_id        UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  bu_id          UUID REFERENCES "__TENANT_SCHEMA__".business_units(bu_id) ON DELETE SET NULL,
  department_id  UUID REFERENCES "__TENANT_SCHEMA__".departments(department_id) ON DELETE SET NULL,
  name_en        TEXT NOT NULL,
  name_ar        TEXT,
  code           TEXT,
  team_type      TEXT,
  function_code  TEXT,
  status         TEXT NOT NULL DEFAULT 'active',
  description    TEXT,
  owner_user_id  VARCHAR(64),
  metadata_json  JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at     TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at     TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_teams_bu       ON "__TENANT_SCHEMA__".teams(bu_id)         WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_teams_dept     ON "__TENANT_SCHEMA__".teams(department_id) WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_teams_function ON "__TENANT_SCHEMA__".teams(function_code) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".team_members (
  team_member_id  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  team_id         UUID NOT NULL REFERENCES "__TENANT_SCHEMA__".teams(team_id) ON DELETE CASCADE,
  user_id         VARCHAR(64) NOT NULL,
  role_in_team    TEXT NOT NULL DEFAULT 'member',
  is_owner        BOOLEAN NOT NULL DEFAULT FALSE,
  joined_at       TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  ended_at        TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (team_id, user_id)
);

CREATE INDEX IF NOT EXISTS idx_tm_user ON "__TENANT_SCHEMA__".team_members(user_id);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".positions (
  position_id  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  bu_id        UUID REFERENCES "__TENANT_SCHEMA__".business_units(bu_id) ON DELETE SET NULL,
  title_en     TEXT NOT NULL,
  title_ar     TEXT,
  code         TEXT,
  grade        TEXT,
  level        INTEGER,
  reports_to   UUID,
  status       TEXT NOT NULL DEFAULT 'active',
  description  TEXT,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at   TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_pos_bu ON "__TENANT_SCHEMA__".positions(bu_id) WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".position_assignments (
  assignment_id  UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  position_id    UUID NOT NULL REFERENCES "__TENANT_SCHEMA__".positions(position_id) ON DELETE CASCADE,
  user_id        VARCHAR(64) NOT NULL,
  is_primary     BOOLEAN NOT NULL DEFAULT TRUE,
  assigned_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  ended_at       TIMESTAMPTZ,
  created_at     TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_posassign_user ON "__TENANT_SCHEMA__".position_assignments(user_id);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".committees (
  committee_id    UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name_en         TEXT NOT NULL,
  name_ar         TEXT,
  code            TEXT,
  committee_type  TEXT,
  charter         TEXT,
  status          TEXT NOT NULL DEFAULT 'active',
  description     TEXT,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at      TIMESTAMPTZ
);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".committee_members (
  member_id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  committee_id       UUID NOT NULL REFERENCES "__TENANT_SCHEMA__".committees(committee_id) ON DELETE CASCADE,
  user_id            VARCHAR(64) NOT NULL,
  role_in_committee  TEXT NOT NULL DEFAULT 'member',
  joined_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  ended_at           TIMESTAMPTZ,
  created_at         TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_cm_user ON "__TENANT_SCHEMA__".committee_members(user_id);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".locations (
  location_id         UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name_en             TEXT NOT NULL,
  name_ar             TEXT,
  code                TEXT,
  location_type       TEXT,
  country             TEXT,
  city                TEXT,
  address             TEXT,
  parent_location_id  UUID,
  latitude            NUMERIC(10,7),
  longitude           NUMERIC(10,7),
  status              TEXT NOT NULL DEFAULT 'active',
  description         TEXT,
  created_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at          TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at          TIMESTAMPTZ
);

CREATE INDEX IF NOT EXISTS idx_loc_parent  ON "__TENANT_SCHEMA__".locations(parent_location_id) WHERE deleted_at IS NULL;

CREATE INDEX IF NOT EXISTS idx_loc_country ON "__TENANT_SCHEMA__".locations(country)            WHERE deleted_at IS NULL;

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".location_bu_map (
  location_id  UUID NOT NULL REFERENCES "__TENANT_SCHEMA__".locations(location_id)         ON DELETE CASCADE,
  bu_id        UUID NOT NULL REFERENCES "__TENANT_SCHEMA__".business_units(bu_id)          ON DELETE CASCADE,
  created_at   TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  PRIMARY KEY (location_id, bu_id)
);

CREATE TABLE IF NOT EXISTS "__TENANT_SCHEMA__".ownership_mappings (
  mapping_id      UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  entity_type     TEXT NOT NULL,
  entity_id       VARCHAR(64) NOT NULL,
  owner_user_id   VARCHAR(64) NOT NULL,
  ownership_type  TEXT NOT NULL DEFAULT 'primary',
  valid_from      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  valid_to        TIMESTAMPTZ,
  created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  UNIQUE (entity_type, entity_id, owner_user_id, ownership_type)
);

CREATE INDEX IF NOT EXISTS idx_own_entity ON "__TENANT_SCHEMA__".ownership_mappings(entity_type, entity_id);
