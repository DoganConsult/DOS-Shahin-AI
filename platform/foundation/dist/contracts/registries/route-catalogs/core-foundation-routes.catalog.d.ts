/**
 * Foundation and organizational structure routes.
 * Covers: workspaces, tenants, users, teams, org hierarchy, departments,
 * locations, positions, roles, profiles, business units, and related
 * foundation-level endpoints.
 */
import type { RouteDefinition } from '../routing/route-definition';
export declare const CORE_FOUNDATION_ROUTES: RouteDefinition[];
