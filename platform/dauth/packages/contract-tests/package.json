{
  "name": "@dos/dauth-contract-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Shared contract-test suite for DAuthPort consumers. Any consumer (product shell, service, module test bed) imports runDAuthPortContract(port) and the assertions fire in its own CI. Detects port drift before it reaches runtime.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@dos/ports": "workspace:*"
  },
  "peerDependencies": {
    "@dos/ports": "^1.0.0",
    "vitest": ">=1.0.0"
  }
}
