export { getActor, registerActor } from './actor-registry';
export type { Actor } from './actor-registry';

export type {
  ActorContext,
  ActorType,
  ActorInfo,
  TenantMembership,
  AccessScopeBinding,
  LandingHint,
  AuditTraceMeta,
} from '@dos/contracts/auth';
