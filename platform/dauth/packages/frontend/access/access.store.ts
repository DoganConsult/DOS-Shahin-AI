/**
 * Re-export shim for `platform/dauth/packages/frontend/**` importing `../access/access.store`.
 * Delegates to the canonical `platform/dauth/access/access.store` shim.
 */
export { AccessStore } from '../../../access/access.store';
