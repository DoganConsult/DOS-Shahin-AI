-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=dauth scope=public function=iam
-- ledger: dos.schema_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- platform/dauth/migrations/public/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (1):
--   * iam_identities
--
-- Source migrations (immutable, 2):
--   * platform/dauth/migrations/public/059_iam_identities.sql
--   * platform/dauth/migrations/public/059_iam_identities_migration_root_variant.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS public.iam_identities (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  -- Matches public.users.user_id (VARCHAR(64) from migration 000);

CREATE INDEX IF NOT EXISTS idx_iam_identities_user
  ON public.iam_identities (user_id);

CREATE INDEX IF NOT EXISTS idx_iam_identities_provider_realm
  ON public.iam_identities (provider, realm);

CREATE TABLE IF NOT EXISTS public.iam_identities (
    id                BIGSERIAL PRIMARY KEY,
    user_id           UUID NOT NULL,
    provider          TEXT NOT NULL,              -- 'keycloak', 'okta', 'azure-ad', ...
    external_subject  TEXT NOT NULL,              -- IdP `sub`
    realm             TEXT,                       -- Keycloak realm (nullable for other IdPs)
    external_username TEXT,
    external_email    TEXT,
    attributes        JSONB NOT NULL DEFAULT '{}'::jsonb,
    status            TEXT NOT NULL DEFAULT 'active'
                      CHECK (status IN ('active','disabled','deleted')),
    created_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at        TIMESTAMPTZ NOT NULL DEFAULT now(),
    CONSTRAINT iam_identities_provider_subject_realm_uk
        UNIQUE (provider, external_subject, realm)
);

CREATE INDEX IF NOT EXISTS iam_identities_user_id_idx
    ON public.iam_identities(user_id);

CREATE INDEX IF NOT EXISTS iam_identities_provider_subject_idx
    ON public.iam_identities(provider, external_subject);

CREATE INDEX IF NOT EXISTS iam_identities_status_idx
    ON public.iam_identities(status)
    WHERE status <> 'active';
