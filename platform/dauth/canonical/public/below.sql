-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=dauth scope=public function=below
-- ledger: dos.schema_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- platform/dauth/migrations/public/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (2):
--   * below
--   * user_role_assignments
--
-- Source migrations (immutable, 1):
--   * platform/dauth/migrations/public/002_user_role_assignments.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS below no-ops against either earlier shape,
-- so we idempotently add every column we need before the indexes.

BEGIN;

CREATE TABLE IF NOT EXISTS dos.user_role_assignments (
  assignment_id VARCHAR(64)  PRIMARY KEY,
  tenant_id     VARCHAR(64)  NOT NULL,
  user_id       VARCHAR(64)  NOT NULL,
  role_code     VARCHAR(100) NOT NULL,
  scope         VARCHAR(255),
  granted_by    VARCHAR(64),
  granted_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  expires_at    TIMESTAMPTZ,
  revoked_at    TIMESTAMPTZ,
  revoked_by    VARCHAR(64),
  is_active     BOOLEAN      NOT NULL DEFAULT TRUE
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_user_role_assignments_active
  ON dos.user_role_assignments (tenant_id, user_id, role_code)
  WHERE is_active = TRUE;

CREATE INDEX IF NOT EXISTS ix_user_role_assignments_tenant_user
  ON dos.user_role_assignments (tenant_id, user_id)
  WHERE is_active = TRUE;

CREATE INDEX IF NOT EXISTS ix_user_role_assignments_tenant_role
  ON dos.user_role_assignments (tenant_id, role_code)
  WHERE is_active = TRUE;
