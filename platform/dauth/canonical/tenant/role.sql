-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=dauth scope=tenant function=role
-- ledger: dos.tenant_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- platform/dauth/migrations/tenant/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (1):
--   * role_permission_map
--
-- Source migrations (immutable, 1):
--   * platform/dauth/migrations/tenant/412_role_permission_map.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.role_permission_map (
  map_id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id         UUID NOT NULL,
  role_code         VARCHAR(50) NOT NULL,
  permission_code   TEXT NOT NULL,
  module_code       TEXT NOT NULL,
  granted_at        TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  granted_by        UUID,
  UNIQUE(tenant_id, role_code, permission_code, module_code)
);

CREATE INDEX IF NOT EXISTS idx_rpm_role ON __TENANT_SCHEMA__.role_permission_map(tenant_id, role_code);

CREATE INDEX IF NOT EXISTS idx_rpm_permission ON __TENANT_SCHEMA__.role_permission_map(tenant_id, permission_code);

CREATE INDEX IF NOT EXISTS idx_rpm_module ON __TENANT_SCHEMA__.role_permission_map(tenant_id, module_code);
