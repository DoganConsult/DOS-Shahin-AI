/**
 * Re-export shim: dauth imports `../access/access.store` (resolved from
 * `platform/dauth/**` → `platform/dauth/access/access.store`).
 * Canonical implementation lives in `@dos/access-store` (dos-access-store).
 */
export { AccessStore } from '../../access/dos-access-store/src/access.store';
