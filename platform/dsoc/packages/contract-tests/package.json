{
  "name": "@dos/dsoc-contract-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Shared contract-test suite for DSOCPort consumers. Any consumer imports runDSOCPortContract(port) and the assertions fire in its own CI.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "dependencies": {
    "@dos/ports": "workspace:*"
  },
  "peerDependencies": {
    "@dos/ports": "^1.0.0",
    "vitest": ">=1.0.0"
  }
}
