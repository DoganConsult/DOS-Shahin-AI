/**
 * Carbon-backed full-screen / overlay loading spinner.
 * Use for blocking page-level loads. For inline / button loads use
 * `<dos-carbon-inline-loading>`.
 */
export declare class DosCarbonLoadingComponent {
    isActive: boolean;
    size: 'sm' | 'normal';
    overlay: boolean;
}
