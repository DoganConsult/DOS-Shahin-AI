/**
 * Carbon-backed toggle-tip (info-button + popover panel). Carbon ships
 * `<cds-toggletip>` element with `[cdsToggletipButton]` and
 * `[cdsToggletipContent]` directive slots.
 *
 * Inputs: id/isOpen.
 */
export declare class DosCarbonToggleTipComponent {
    isOpen: boolean;
    buttonLabel: string;
}
