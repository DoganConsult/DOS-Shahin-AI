{
  "name": "@dos/ui-os-client",
  "version": "1.0.0",
  "description": "DOS Platform — UI-OS HTTP client + component allowlists. Single canonical Angular surface for the /api/ui-os/* contract owned by services/ui-os-service. Replaces any prior dynamic-ui FE client; products consume from this entry only.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "peerDependencies": {
    "@angular/common": ">=20.0.0 <22.0.0",
    "@angular/core": ">=20.0.0 <22.0.0",
    "rxjs": ">=7.0.0"
  },
  "devDependencies": {
    "@angular/common": "^21.2.7",
    "@angular/core": "^21.2.7",
    "rxjs": "^7.8.1",
    "typescript": "^5.7.0"
  }
}
