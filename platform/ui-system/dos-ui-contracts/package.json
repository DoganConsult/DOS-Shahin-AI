{
  "name": "@dos/ui-contracts",
  "version": "1.0.0",
  "description": "DOS Platform — UI contracts (Page, Navigation, Action, Responsive, Component keys). Pure TypeScript types consumed by Dynamic UI and product apps.",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": { "types": "./src/index.ts", "default": "./src/index.ts" }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
