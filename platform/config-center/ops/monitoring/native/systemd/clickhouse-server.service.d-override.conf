# Drop-in override for the clickhouse-server unit shipped by the official package.
# Install location: /etc/systemd/system/clickhouse-server.service.d/override.conf
# The install.sh script copies this file into place.

[Service]
LimitNOFILE=262144
Environment=CLICKHOUSE_DB=dos_analytics
