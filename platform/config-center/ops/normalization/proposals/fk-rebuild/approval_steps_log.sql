-- FK rebuild proposals for approval_steps_log
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] approval_steps_log.log_id -> action_change_log.id
ALTER TABLE public.approval_steps_log
  ADD CONSTRAINT fk_approval_steps_log_log_id
  FOREIGN KEY (log_id)
  REFERENCES public.action_change_log(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_steps_log VALIDATE CONSTRAINT fk_approval_steps_log_log_id;

-- [MEDIUM] approval_steps_log.request_id -> approval_requests.request_id
ALTER TABLE public.approval_steps_log
  ADD CONSTRAINT fk_approval_steps_log_request_id
  FOREIGN KEY (request_id)
  REFERENCES public.approval_requests(request_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_steps_log VALIDATE CONSTRAINT fk_approval_steps_log_request_id;

-- [HIGH] approval_steps_log.actor_id -> actors.actor_id
ALTER TABLE public.approval_steps_log
  ADD CONSTRAINT fk_approval_steps_log_actor_id
  FOREIGN KEY (actor_id)
  REFERENCES public.actors(actor_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_steps_log VALIDATE CONSTRAINT fk_approval_steps_log_actor_id;
