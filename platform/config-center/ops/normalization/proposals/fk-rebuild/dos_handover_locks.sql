-- FK rebuild proposals for dos_handover_locks
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] dos_handover_locks.lock_id -> config_locks.id
ALTER TABLE public.dos_handover_locks
  ADD CONSTRAINT fk_dos_handover_locks_lock_id
  FOREIGN KEY (lock_id)
  REFERENCES public.config_locks(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_handover_locks VALIDATE CONSTRAINT fk_dos_handover_locks_lock_id;

-- [MEDIUM] dos_handover_locks.release_id -> dos_releases.release_id
ALTER TABLE public.dos_handover_locks
  ADD CONSTRAINT fk_dos_handover_locks_release_id
  FOREIGN KEY (release_id)
  REFERENCES public.dos_releases(release_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_handover_locks VALIDATE CONSTRAINT fk_dos_handover_locks_release_id;
