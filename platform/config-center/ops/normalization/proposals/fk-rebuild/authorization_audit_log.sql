-- FK rebuild proposals for authorization_audit_log
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] authorization_audit_log.audit_id -> module_config_audit.id
ALTER TABLE public.authorization_audit_log
  ADD CONSTRAINT fk_authorization_audit_log_audit_id
  FOREIGN KEY (audit_id)
  REFERENCES public.module_config_audit(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.authorization_audit_log VALIDATE CONSTRAINT fk_authorization_audit_log_audit_id;

-- [HIGH] authorization_audit_log.user_id -> users.user_id
ALTER TABLE public.authorization_audit_log
  ADD CONSTRAINT fk_authorization_audit_log_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.authorization_audit_log VALIDATE CONSTRAINT fk_authorization_audit_log_user_id;

-- [MEDIUM] authorization_audit_log.resource_id -> audit_resources.id
ALTER TABLE public.authorization_audit_log
  ADD CONSTRAINT fk_authorization_audit_log_resource_id
  FOREIGN KEY (resource_id)
  REFERENCES public.audit_resources(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.authorization_audit_log VALIDATE CONSTRAINT fk_authorization_audit_log_resource_id;

-- [LOW] authorization_audit_log.matched_role_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.
