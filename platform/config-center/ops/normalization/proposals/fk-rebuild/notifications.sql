-- FK rebuild proposals for notifications
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] notifications.notification_id -> notifications.notification_id
ALTER TABLE public.notifications
  ADD CONSTRAINT fk_notifications_notification_id
  FOREIGN KEY (notification_id)
  REFERENCES public.notifications(notification_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.notifications VALIDATE CONSTRAINT fk_notifications_notification_id;

-- [MEDIUM] notifications.recipient_id -> reporting_recipients.id
ALTER TABLE public.notifications
  ADD CONSTRAINT fk_notifications_recipient_id
  FOREIGN KEY (recipient_id)
  REFERENCES public.reporting_recipients(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.notifications VALIDATE CONSTRAINT fk_notifications_recipient_id;
