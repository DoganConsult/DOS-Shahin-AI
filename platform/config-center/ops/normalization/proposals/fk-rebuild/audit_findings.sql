-- FK rebuild proposals for audit_findings
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] audit_findings.finding_id -> findings.finding_id
ALTER TABLE public.audit_findings
  ADD CONSTRAINT fk_audit_findings_finding_id
  FOREIGN KEY (finding_id)
  REFERENCES public.findings(finding_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_findings VALIDATE CONSTRAINT fk_audit_findings_finding_id;

-- [HIGH] audit_findings.control_id -> controls.control_id
ALTER TABLE public.audit_findings
  ADD CONSTRAINT fk_audit_findings_control_id
  FOREIGN KEY (control_id)
  REFERENCES public.controls(control_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_findings VALIDATE CONSTRAINT fk_audit_findings_control_id;
