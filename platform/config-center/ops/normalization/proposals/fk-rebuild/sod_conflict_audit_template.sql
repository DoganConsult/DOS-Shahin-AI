-- FK rebuild proposals for sod_conflict_audit_template
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] sod_conflict_audit_template.user_id -> users.user_id
ALTER TABLE public.sod_conflict_audit_template
  ADD CONSTRAINT fk_sod_conflict_audit_template_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.sod_conflict_audit_template VALIDATE CONSTRAINT fk_sod_conflict_audit_template_user_id;
