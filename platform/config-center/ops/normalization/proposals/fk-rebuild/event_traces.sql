-- FK rebuild proposals for event_traces
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] event_traces.event_id -> compliance_calendar_events.id
ALTER TABLE public.event_traces
  ADD CONSTRAINT fk_event_traces_event_id
  FOREIGN KEY (event_id)
  REFERENCES public.compliance_calendar_events(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.event_traces VALIDATE CONSTRAINT fk_event_traces_event_id;

-- [MEDIUM] event_traces.correlation_id -> risk_correlations.id
ALTER TABLE public.event_traces
  ADD CONSTRAINT fk_event_traces_correlation_id
  FOREIGN KEY (correlation_id)
  REFERENCES public.risk_correlations(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.event_traces VALIDATE CONSTRAINT fk_event_traces_correlation_id;
