-- FK rebuild proposals for risk_approval_requests
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] risk_approval_requests.approval_id -> action_approvals.id
ALTER TABLE __TENANT_SCHEMA__.risk_approval_requests
  ADD CONSTRAINT fk_risk_approval_requests_approval_id
  FOREIGN KEY (approval_id)
  REFERENCES __TENANT_SCHEMA__.action_approvals(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.risk_approval_requests VALIDATE CONSTRAINT fk_risk_approval_requests_approval_id;

-- [HIGH] risk_approval_requests.risk_id -> risks.risk_id
ALTER TABLE __TENANT_SCHEMA__.risk_approval_requests
  ADD CONSTRAINT fk_risk_approval_requests_risk_id
  FOREIGN KEY (risk_id)
  REFERENCES __TENANT_SCHEMA__.risks(risk_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.risk_approval_requests VALIDATE CONSTRAINT fk_risk_approval_requests_risk_id;
