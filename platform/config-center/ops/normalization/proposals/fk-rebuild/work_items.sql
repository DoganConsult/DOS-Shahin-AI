-- FK rebuild proposals for work_items
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] work_items.work_item_id -> work_items.work_item_id
ALTER TABLE public.work_items
  ADD CONSTRAINT fk_work_items_work_item_id
  FOREIGN KEY (work_item_id)
  REFERENCES public.work_items(work_item_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.work_items VALIDATE CONSTRAINT fk_work_items_work_item_id;

-- [MEDIUM] work_items.source_id -> analytics_data_sources.id
ALTER TABLE public.work_items
  ADD CONSTRAINT fk_work_items_source_id
  FOREIGN KEY (source_id)
  REFERENCES public.analytics_data_sources(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.work_items VALIDATE CONSTRAINT fk_work_items_source_id;

-- [HIGH] work_items.entity_id -> entities.id
ALTER TABLE public.work_items
  ADD CONSTRAINT fk_work_items_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.work_items VALIDATE CONSTRAINT fk_work_items_entity_id;
