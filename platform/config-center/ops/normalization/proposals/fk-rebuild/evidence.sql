-- FK rebuild proposals for evidence
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] evidence.evidence_id -> evidence.evidence_id
ALTER TABLE public.evidence
  ADD CONSTRAINT fk_evidence_evidence_id
  FOREIGN KEY (evidence_id)
  REFERENCES public.evidence(evidence_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.evidence VALIDATE CONSTRAINT fk_evidence_evidence_id;

-- [HIGH] evidence.control_id -> controls.control_id
ALTER TABLE public.evidence
  ADD CONSTRAINT fk_evidence_control_id
  FOREIGN KEY (control_id)
  REFERENCES public.controls(control_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.evidence VALIDATE CONSTRAINT fk_evidence_control_id;
