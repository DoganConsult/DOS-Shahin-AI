-- FK rebuild proposals for training_courses
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] training_courses.course_id -> training_courses.course_id
ALTER TABLE public.training_courses
  ADD CONSTRAINT fk_training_courses_course_id
  FOREIGN KEY (course_id)
  REFERENCES public.training_courses(course_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.training_courses VALIDATE CONSTRAINT fk_training_courses_course_id;
