-- FK rebuild proposals for controls
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] controls.control_id -> controls.control_id
ALTER TABLE public.controls
  ADD CONSTRAINT fk_controls_control_id
  FOREIGN KEY (control_id)
  REFERENCES public.controls(control_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.controls VALIDATE CONSTRAINT fk_controls_control_id;

-- [HIGH] controls.framework_id -> frameworks.framework_id
ALTER TABLE public.controls
  ADD CONSTRAINT fk_controls_framework_id
  FOREIGN KEY (framework_id)
  REFERENCES public.frameworks(framework_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.controls VALIDATE CONSTRAINT fk_controls_framework_id;

-- [MEDIUM] controls.owner_id -> risk_owners.id
ALTER TABLE public.controls
  ADD CONSTRAINT fk_controls_owner_id
  FOREIGN KEY (owner_id)
  REFERENCES public.risk_owners(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.controls VALIDATE CONSTRAINT fk_controls_owner_id;
