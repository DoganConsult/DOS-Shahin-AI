-- FK rebuild proposals for bcp_plans
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] bcp_plans.plan_id -> audit_plan.plan_id
ALTER TABLE public.bcp_plans
  ADD CONSTRAINT fk_bcp_plans_plan_id
  FOREIGN KEY (plan_id)
  REFERENCES public.audit_plan(plan_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.bcp_plans VALIDATE CONSTRAINT fk_bcp_plans_plan_id;

-- [MEDIUM] bcp_plans.owner_id -> risk_owners.id
ALTER TABLE public.bcp_plans
  ADD CONSTRAINT fk_bcp_plans_owner_id
  FOREIGN KEY (owner_id)
  REFERENCES public.risk_owners(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.bcp_plans VALIDATE CONSTRAINT fk_bcp_plans_owner_id;
