-- FK rebuild proposals for risks
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] risks.risk_id -> risks.risk_id
ALTER TABLE public.risks
  ADD CONSTRAINT fk_risks_risk_id
  FOREIGN KEY (risk_id)
  REFERENCES public.risks(risk_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.risks VALIDATE CONSTRAINT fk_risks_risk_id;

-- [MEDIUM] risks.owner_id -> risk_owners.id
ALTER TABLE public.risks
  ADD CONSTRAINT fk_risks_owner_id
  FOREIGN KEY (owner_id)
  REFERENCES public.risk_owners(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.risks VALIDATE CONSTRAINT fk_risks_owner_id;
