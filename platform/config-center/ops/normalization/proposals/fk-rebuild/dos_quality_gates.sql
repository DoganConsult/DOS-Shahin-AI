-- FK rebuild proposals for dos_quality_gates
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] dos_quality_gates.gate_id -> dos_feature_gates.gate_code
ALTER TABLE public.dos_quality_gates
  ADD CONSTRAINT fk_dos_quality_gates_gate_id
  FOREIGN KEY (gate_id)
  REFERENCES public.dos_feature_gates(gate_code)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_quality_gates VALIDATE CONSTRAINT fk_dos_quality_gates_gate_id;

-- [MEDIUM] dos_quality_gates.release_id -> dos_releases.release_id
ALTER TABLE public.dos_quality_gates
  ADD CONSTRAINT fk_dos_quality_gates_release_id
  FOREIGN KEY (release_id)
  REFERENCES public.dos_releases(release_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_quality_gates VALIDATE CONSTRAINT fk_dos_quality_gates_release_id;
