-- FK rebuild proposals for audit_change_log
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] audit_change_log.entity_id -> entities.id
ALTER TABLE __TENANT_SCHEMA__.audit_change_log
  ADD CONSTRAINT fk_audit_change_log_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES __TENANT_SCHEMA__.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.audit_change_log VALIDATE CONSTRAINT fk_audit_change_log_entity_id;

-- [MEDIUM] audit_change_log.correlation_id -> risk_correlations.id
ALTER TABLE __TENANT_SCHEMA__.audit_change_log
  ADD CONSTRAINT fk_audit_change_log_correlation_id
  FOREIGN KEY (correlation_id)
  REFERENCES __TENANT_SCHEMA__.risk_correlations(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.audit_change_log VALIDATE CONSTRAINT fk_audit_change_log_correlation_id;
