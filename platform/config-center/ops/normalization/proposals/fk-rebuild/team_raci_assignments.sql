-- FK rebuild proposals for team_raci_assignments
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] team_raci_assignments.team_id -> teams.id
ALTER TABLE public.team_raci_assignments
  ADD CONSTRAINT fk_team_raci_assignments_team_id
  FOREIGN KEY (team_id)
  REFERENCES public.teams(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.team_raci_assignments VALIDATE CONSTRAINT fk_team_raci_assignments_team_id;

-- [HIGH] team_raci_assignments.user_id -> users.user_id
ALTER TABLE public.team_raci_assignments
  ADD CONSTRAINT fk_team_raci_assignments_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.team_raci_assignments VALIDATE CONSTRAINT fk_team_raci_assignments_user_id;

-- [MEDIUM] team_raci_assignments.scope_id -> compliance_scope.id
ALTER TABLE public.team_raci_assignments
  ADD CONSTRAINT fk_team_raci_assignments_scope_id
  FOREIGN KEY (scope_id)
  REFERENCES public.compliance_scope(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.team_raci_assignments VALIDATE CONSTRAINT fk_team_raci_assignments_scope_id;
