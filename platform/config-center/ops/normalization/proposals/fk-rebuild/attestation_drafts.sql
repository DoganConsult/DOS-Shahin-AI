-- FK rebuild proposals for attestation_drafts
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] attestation_drafts.draft_id -> attestation_drafts.draft_id
ALTER TABLE __TENANT_SCHEMA__.attestation_drafts
  ADD CONSTRAINT fk_attestation_drafts_draft_id
  FOREIGN KEY (draft_id)
  REFERENCES __TENANT_SCHEMA__.attestation_drafts(draft_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.attestation_drafts VALIDATE CONSTRAINT fk_attestation_drafts_draft_id;

-- [HIGH] attestation_drafts.entity_id -> entities.id
ALTER TABLE __TENANT_SCHEMA__.attestation_drafts
  ADD CONSTRAINT fk_attestation_drafts_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES __TENANT_SCHEMA__.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.attestation_drafts VALIDATE CONSTRAINT fk_attestation_drafts_entity_id;
