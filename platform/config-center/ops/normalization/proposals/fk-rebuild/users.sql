-- FK rebuild proposals for users
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] users.user_id -> users.user_id
ALTER TABLE public.users
  ADD CONSTRAINT fk_users_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.users VALIDATE CONSTRAINT fk_users_user_id;

-- [LOW] users.agent_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.

-- [HIGH] users.department_id -> departments.id
ALTER TABLE public.users
  ADD CONSTRAINT fk_users_department_id
  FOREIGN KEY (department_id)
  REFERENCES public.departments(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.users VALIDATE CONSTRAINT fk_users_department_id;

-- [LOW] users.manager_user_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.
