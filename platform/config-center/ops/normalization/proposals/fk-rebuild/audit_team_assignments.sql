-- FK rebuild proposals for audit_team_assignments
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] audit_team_assignments.engagement_id -> audit_engagements.id
ALTER TABLE __TENANT_SCHEMA__.audit_team_assignments
  ADD CONSTRAINT fk_audit_team_assignments_engagement_id
  FOREIGN KEY (engagement_id)
  REFERENCES __TENANT_SCHEMA__.audit_engagements(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.audit_team_assignments VALIDATE CONSTRAINT fk_audit_team_assignments_engagement_id;

-- [HIGH] audit_team_assignments.user_id -> users.user_id
ALTER TABLE __TENANT_SCHEMA__.audit_team_assignments
  ADD CONSTRAINT fk_audit_team_assignments_user_id
  FOREIGN KEY (user_id)
  REFERENCES __TENANT_SCHEMA__.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.audit_team_assignments VALIDATE CONSTRAINT fk_audit_team_assignments_user_id;
