-- FK rebuild proposals for password_reset_tokens
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] password_reset_tokens.token_id -> email_verification_tokens.id
ALTER TABLE public.password_reset_tokens
  ADD CONSTRAINT fk_password_reset_tokens_token_id
  FOREIGN KEY (token_id)
  REFERENCES public.email_verification_tokens(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.password_reset_tokens VALIDATE CONSTRAINT fk_password_reset_tokens_token_id;

-- [HIGH] password_reset_tokens.user_id -> users.user_id
ALTER TABLE public.password_reset_tokens
  ADD CONSTRAINT fk_password_reset_tokens_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.password_reset_tokens VALIDATE CONSTRAINT fk_password_reset_tokens_user_id;
