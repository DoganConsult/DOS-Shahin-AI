-- FK rebuild proposals for workflow_scheduled_jobs
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] workflow_scheduled_jobs.job_id -> export_jobs.id
ALTER TABLE public.workflow_scheduled_jobs
  ADD CONSTRAINT fk_workflow_scheduled_jobs_job_id
  FOREIGN KEY (job_id)
  REFERENCES public.export_jobs(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.workflow_scheduled_jobs VALIDATE CONSTRAINT fk_workflow_scheduled_jobs_job_id;
