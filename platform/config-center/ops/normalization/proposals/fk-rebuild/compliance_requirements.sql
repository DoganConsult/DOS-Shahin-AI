-- FK rebuild proposals for compliance_requirements
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] compliance_requirements.requirement_id -> compliance_requirements.requirement_id
ALTER TABLE public.compliance_requirements
  ADD CONSTRAINT fk_compliance_requirements_requirement_id
  FOREIGN KEY (requirement_id)
  REFERENCES public.compliance_requirements(requirement_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.compliance_requirements VALIDATE CONSTRAINT fk_compliance_requirements_requirement_id;

-- [HIGH] compliance_requirements.framework_id -> frameworks.framework_id
ALTER TABLE public.compliance_requirements
  ADD CONSTRAINT fk_compliance_requirements_framework_id
  FOREIGN KEY (framework_id)
  REFERENCES public.frameworks(framework_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.compliance_requirements VALIDATE CONSTRAINT fk_compliance_requirements_framework_id;

-- [MEDIUM] compliance_requirements.owner_id -> risk_owners.id
ALTER TABLE public.compliance_requirements
  ADD CONSTRAINT fk_compliance_requirements_owner_id
  FOREIGN KEY (owner_id)
  REFERENCES public.risk_owners(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.compliance_requirements VALIDATE CONSTRAINT fk_compliance_requirements_owner_id;
