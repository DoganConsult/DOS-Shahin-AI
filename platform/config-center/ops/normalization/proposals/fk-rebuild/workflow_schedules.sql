-- FK rebuild proposals for workflow_schedules
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] workflow_schedules.schedule_id -> bcp_exercise_schedule.id
ALTER TABLE public.workflow_schedules
  ADD CONSTRAINT fk_workflow_schedules_schedule_id
  FOREIGN KEY (schedule_id)
  REFERENCES public.bcp_exercise_schedule(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.workflow_schedules VALIDATE CONSTRAINT fk_workflow_schedules_schedule_id;

-- [HIGH] workflow_schedules.workflow_template_id -> workflow_templates.template_id
ALTER TABLE public.workflow_schedules
  ADD CONSTRAINT fk_workflow_schedules_workflow_template_id
  FOREIGN KEY (workflow_template_id)
  REFERENCES public.workflow_templates(template_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.workflow_schedules VALIDATE CONSTRAINT fk_workflow_schedules_workflow_template_id;
