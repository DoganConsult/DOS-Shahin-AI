-- FK rebuild proposals for user_role_assignments
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] user_role_assignments.user_id -> users.user_id
ALTER TABLE public.user_role_assignments
  ADD CONSTRAINT fk_user_role_assignments_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.user_role_assignments VALIDATE CONSTRAINT fk_user_role_assignments_user_id;

-- [HIGH] user_role_assignments.functional_role_id -> functional_roles.id
ALTER TABLE public.user_role_assignments
  ADD CONSTRAINT fk_user_role_assignments_functional_role_id
  FOREIGN KEY (functional_role_id)
  REFERENCES public.functional_roles(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.user_role_assignments VALIDATE CONSTRAINT fk_user_role_assignments_functional_role_id;
