-- FK rebuild proposals for audit_trail
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] audit_trail.user_id -> users.user_id
ALTER TABLE public.audit_trail
  ADD CONSTRAINT fk_audit_trail_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_trail VALIDATE CONSTRAINT fk_audit_trail_user_id;

-- [HIGH] audit_trail.entity_id -> entities.id
ALTER TABLE public.audit_trail
  ADD CONSTRAINT fk_audit_trail_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_trail VALIDATE CONSTRAINT fk_audit_trail_entity_id;
