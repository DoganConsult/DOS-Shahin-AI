-- FK rebuild proposals for training_certifications
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] training_certifications.user_id -> users.user_id
ALTER TABLE __TENANT_SCHEMA__.training_certifications
  ADD CONSTRAINT fk_training_certifications_user_id
  FOREIGN KEY (user_id)
  REFERENCES __TENANT_SCHEMA__.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.training_certifications VALIDATE CONSTRAINT fk_training_certifications_user_id;

-- [MEDIUM] training_certifications.course_id -> training_courses.course_id
ALTER TABLE __TENANT_SCHEMA__.training_certifications
  ADD CONSTRAINT fk_training_certifications_course_id
  FOREIGN KEY (course_id)
  REFERENCES __TENANT_SCHEMA__.training_courses(course_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.training_certifications VALIDATE CONSTRAINT fk_training_certifications_course_id;
