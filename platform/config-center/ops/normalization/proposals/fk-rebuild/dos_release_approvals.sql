-- FK rebuild proposals for dos_release_approvals
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] dos_release_approvals.approval_id -> action_approvals.id
ALTER TABLE public.dos_release_approvals
  ADD CONSTRAINT fk_dos_release_approvals_approval_id
  FOREIGN KEY (approval_id)
  REFERENCES public.action_approvals(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_release_approvals VALIDATE CONSTRAINT fk_dos_release_approvals_approval_id;

-- [MEDIUM] dos_release_approvals.release_id -> dos_releases.release_id
ALTER TABLE public.dos_release_approvals
  ADD CONSTRAINT fk_dos_release_approvals_release_id
  FOREIGN KEY (release_id)
  REFERENCES public.dos_releases(release_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_release_approvals VALIDATE CONSTRAINT fk_dos_release_approvals_release_id;
