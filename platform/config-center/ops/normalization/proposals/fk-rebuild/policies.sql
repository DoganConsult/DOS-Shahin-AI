-- FK rebuild proposals for policies
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] policies.policy_id -> policies.policy_id
ALTER TABLE public.policies
  ADD CONSTRAINT fk_policies_policy_id
  FOREIGN KEY (policy_id)
  REFERENCES public.policies(policy_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.policies VALIDATE CONSTRAINT fk_policies_policy_id;

-- [MEDIUM] policies.owner_id -> risk_owners.id
ALTER TABLE public.policies
  ADD CONSTRAINT fk_policies_owner_id
  FOREIGN KEY (owner_id)
  REFERENCES public.risk_owners(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.policies VALIDATE CONSTRAINT fk_policies_owner_id;
