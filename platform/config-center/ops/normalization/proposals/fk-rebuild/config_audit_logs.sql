-- FK rebuild proposals for config_audit_logs
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] config_audit_logs.definition_id -> alert_definitions.alert_id
ALTER TABLE public.config_audit_logs
  ADD CONSTRAINT fk_config_audit_logs_definition_id
  FOREIGN KEY (definition_id)
  REFERENCES public.alert_definitions(alert_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.config_audit_logs VALIDATE CONSTRAINT fk_config_audit_logs_definition_id;

-- [MEDIUM] config_audit_logs.scope_id -> compliance_scope.id
ALTER TABLE public.config_audit_logs
  ADD CONSTRAINT fk_config_audit_logs_scope_id
  FOREIGN KEY (scope_id)
  REFERENCES public.compliance_scope(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.config_audit_logs VALIDATE CONSTRAINT fk_config_audit_logs_scope_id;

-- [LOW] config_audit_logs.actor_user_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.
