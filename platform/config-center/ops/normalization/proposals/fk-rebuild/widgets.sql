-- FK rebuild proposals for widgets
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] widgets.widget_id -> widgets.widget_id
ALTER TABLE public.widgets
  ADD CONSTRAINT fk_widgets_widget_id
  FOREIGN KEY (widget_id)
  REFERENCES public.widgets(widget_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.widgets VALIDATE CONSTRAINT fk_widgets_widget_id;

-- [HIGH] widgets.dashboard_id -> dashboards.dashboard_id
ALTER TABLE public.widgets
  ADD CONSTRAINT fk_widgets_dashboard_id
  FOREIGN KEY (dashboard_id)
  REFERENCES public.dashboards(dashboard_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.widgets VALIDATE CONSTRAINT fk_widgets_dashboard_id;
