-- FK rebuild proposals for approval_requests
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] approval_requests.request_id -> approval_requests.request_id
ALTER TABLE public.approval_requests
  ADD CONSTRAINT fk_approval_requests_request_id
  FOREIGN KEY (request_id)
  REFERENCES public.approval_requests(request_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_requests VALIDATE CONSTRAINT fk_approval_requests_request_id;

-- [MEDIUM] approval_requests.chain_id -> approval_chains.chain_id
ALTER TABLE public.approval_requests
  ADD CONSTRAINT fk_approval_requests_chain_id
  FOREIGN KEY (chain_id)
  REFERENCES public.approval_chains(chain_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_requests VALIDATE CONSTRAINT fk_approval_requests_chain_id;

-- [HIGH] approval_requests.entity_id -> entities.id
ALTER TABLE public.approval_requests
  ADD CONSTRAINT fk_approval_requests_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.approval_requests VALIDATE CONSTRAINT fk_approval_requests_entity_id;
