-- FK rebuild proposals for alert_definitions
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] alert_definitions.alert_id -> action_alerts.id
ALTER TABLE public.alert_definitions
  ADD CONSTRAINT fk_alert_definitions_alert_id
  FOREIGN KEY (alert_id)
  REFERENCES public.action_alerts(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.alert_definitions VALIDATE CONSTRAINT fk_alert_definitions_alert_id;

-- [MEDIUM] alert_definitions.runbook_id -> os_runbooks.id
ALTER TABLE public.alert_definitions
  ADD CONSTRAINT fk_alert_definitions_runbook_id
  FOREIGN KEY (runbook_id)
  REFERENCES public.os_runbooks(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.alert_definitions VALIDATE CONSTRAINT fk_alert_definitions_runbook_id;
