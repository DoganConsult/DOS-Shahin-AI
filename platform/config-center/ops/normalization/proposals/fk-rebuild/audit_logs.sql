-- FK rebuild proposals for audit_logs
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] audit_logs.log_id -> action_change_log.id
ALTER TABLE public.audit_logs
  ADD CONSTRAINT fk_audit_logs_log_id
  FOREIGN KEY (log_id)
  REFERENCES public.action_change_log(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_logs VALIDATE CONSTRAINT fk_audit_logs_log_id;

-- [HIGH] audit_logs.entity_id -> entities.id
ALTER TABLE public.audit_logs
  ADD CONSTRAINT fk_audit_logs_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_logs VALIDATE CONSTRAINT fk_audit_logs_entity_id;

-- [HIGH] audit_logs.actor_id -> actors.actor_id
ALTER TABLE public.audit_logs
  ADD CONSTRAINT fk_audit_logs_actor_id
  FOREIGN KEY (actor_id)
  REFERENCES public.actors(actor_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_logs VALIDATE CONSTRAINT fk_audit_logs_actor_id;
