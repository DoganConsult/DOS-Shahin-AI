-- FK rebuild proposals for config_locks
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] config_locks.definition_id -> alert_definitions.alert_id
ALTER TABLE public.config_locks
  ADD CONSTRAINT fk_config_locks_definition_id
  FOREIGN KEY (definition_id)
  REFERENCES public.alert_definitions(alert_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.config_locks VALIDATE CONSTRAINT fk_config_locks_definition_id;

-- [LOW] config_locks.locked_at_scope_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.
