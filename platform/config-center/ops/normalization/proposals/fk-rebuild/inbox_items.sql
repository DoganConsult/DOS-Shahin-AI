-- FK rebuild proposals for inbox_items
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] inbox_items.item_id -> action_items.id
ALTER TABLE public.inbox_items
  ADD CONSTRAINT fk_inbox_items_item_id
  FOREIGN KEY (item_id)
  REFERENCES public.action_items(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.inbox_items VALIDATE CONSTRAINT fk_inbox_items_item_id;

-- [HIGH] inbox_items.user_id -> users.user_id
ALTER TABLE public.inbox_items
  ADD CONSTRAINT fk_inbox_items_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.inbox_items VALIDATE CONSTRAINT fk_inbox_items_user_id;

-- [HIGH] inbox_items.entity_id -> entities.id
ALTER TABLE public.inbox_items
  ADD CONSTRAINT fk_inbox_items_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.inbox_items VALIDATE CONSTRAINT fk_inbox_items_entity_id;
