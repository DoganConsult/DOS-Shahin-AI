-- FK rebuild proposals for dos_cutover_plans
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [LOW] dos_cutover_plans.cutover_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.

-- [MEDIUM] dos_cutover_plans.release_id -> dos_releases.release_id
ALTER TABLE public.dos_cutover_plans
  ADD CONSTRAINT fk_dos_cutover_plans_release_id
  FOREIGN KEY (release_id)
  REFERENCES public.dos_releases(release_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_cutover_plans VALIDATE CONSTRAINT fk_dos_cutover_plans_release_id;
