-- FK rebuild proposals for risk_reviews
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] risk_reviews.review_id -> audit_quality_review.id
ALTER TABLE __TENANT_SCHEMA__.risk_reviews
  ADD CONSTRAINT fk_risk_reviews_review_id
  FOREIGN KEY (review_id)
  REFERENCES __TENANT_SCHEMA__.audit_quality_review(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.risk_reviews VALIDATE CONSTRAINT fk_risk_reviews_review_id;

-- [HIGH] risk_reviews.risk_id -> risks.risk_id
ALTER TABLE __TENANT_SCHEMA__.risk_reviews
  ADD CONSTRAINT fk_risk_reviews_risk_id
  FOREIGN KEY (risk_id)
  REFERENCES __TENANT_SCHEMA__.risks(risk_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE __TENANT_SCHEMA__.risk_reviews VALIDATE CONSTRAINT fk_risk_reviews_risk_id;
