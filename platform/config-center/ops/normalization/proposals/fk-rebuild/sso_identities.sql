-- FK rebuild proposals for sso_identities
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [HIGH] sso_identities.user_id -> users.user_id
ALTER TABLE public.sso_identities
  ADD CONSTRAINT fk_sso_identities_user_id
  FOREIGN KEY (user_id)
  REFERENCES public.users(user_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.sso_identities VALIDATE CONSTRAINT fk_sso_identities_user_id;

-- [MEDIUM] sso_identities.external_id -> audit_external.id
ALTER TABLE public.sso_identities
  ADD CONSTRAINT fk_sso_identities_external_id
  FOREIGN KEY (external_id)
  REFERENCES public.audit_external(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.sso_identities VALIDATE CONSTRAINT fk_sso_identities_external_id;
