-- FK rebuild proposals for config_values
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] config_values.definition_id -> alert_definitions.alert_id
ALTER TABLE public.config_values
  ADD CONSTRAINT fk_config_values_definition_id
  FOREIGN KEY (definition_id)
  REFERENCES public.alert_definitions(alert_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.config_values VALIDATE CONSTRAINT fk_config_values_definition_id;

-- [MEDIUM] config_values.scope_id -> compliance_scope.id
ALTER TABLE public.config_values
  ADD CONSTRAINT fk_config_values_scope_id
  FOREIGN KEY (scope_id)
  REFERENCES public.compliance_scope(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.config_values VALIDATE CONSTRAINT fk_config_values_scope_id;
