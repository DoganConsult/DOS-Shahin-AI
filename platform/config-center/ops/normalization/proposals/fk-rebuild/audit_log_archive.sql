-- FK rebuild proposals for audit_log_archive
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [LOW] audit_log_archive.entry_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.

-- [HIGH] audit_log_archive.actor_id -> actors.actor_id
ALTER TABLE public.audit_log_archive
  ADD CONSTRAINT fk_audit_log_archive_actor_id
  FOREIGN KEY (actor_id)
  REFERENCES public.actors(actor_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_log_archive VALIDATE CONSTRAINT fk_audit_log_archive_actor_id;

-- [HIGH] audit_log_archive.entity_id -> entities.id
ALTER TABLE public.audit_log_archive
  ADD CONSTRAINT fk_audit_log_archive_entity_id
  FOREIGN KEY (entity_id)
  REFERENCES public.entities(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.audit_log_archive VALIDATE CONSTRAINT fk_audit_log_archive_entity_id;
