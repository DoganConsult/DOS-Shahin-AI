-- FK rebuild proposals for processed_events
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] processed_events.event_id -> compliance_calendar_events.id
ALTER TABLE public.processed_events
  ADD CONSTRAINT fk_processed_events_event_id
  FOREIGN KEY (event_id)
  REFERENCES public.compliance_calendar_events(id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.processed_events VALIDATE CONSTRAINT fk_processed_events_event_id;
