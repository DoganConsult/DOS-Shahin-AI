-- FK rebuild proposals for dos_verification_runs
-- Generated by ops/normalization/scripts/fk-rebuild-generate.ts
-- DO NOT APPLY without review. Each FK below is a separate decision.
-- Workflow: (1) ALTER ... NOT VALID, then (2) VALIDATE during low-traffic.

-- [MEDIUM] dos_verification_runs.run_id -> dos_verification_runs.run_id
ALTER TABLE public.dos_verification_runs
  ADD CONSTRAINT fk_dos_verification_runs_run_id
  FOREIGN KEY (run_id)
  REFERENCES public.dos_verification_runs(run_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_verification_runs VALIDATE CONSTRAINT fk_dos_verification_runs_run_id;

-- [MEDIUM] dos_verification_runs.release_id -> dos_releases.release_id
ALTER TABLE public.dos_verification_runs
  ADD CONSTRAINT fk_dos_verification_runs_release_id
  FOREIGN KEY (release_id)
  REFERENCES public.dos_releases(release_id)
  -- ON DELETE/UPDATE: choose explicitly per FK. Default left blank = NO ACTION.
  NOT VALID;
-- Then, in a separate window:
-- ALTER TABLE public.dos_verification_runs VALIDATE CONSTRAINT fk_dos_verification_runs_release_id;

-- [LOW] dos_verification_runs.deployment_id -> ???.???
-- SKIPPED: no target table inferred. Decide manually before adding.
