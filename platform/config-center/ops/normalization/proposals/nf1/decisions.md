# 1NF Fix — Decision Matrix
Generated: 2026-04-20T22:40:10.121Z
Total 1NF violations: **310** across 223 tables.

| Table | Layer | Violation | Column(s) | Proposed fix | Decision |
|---|---|---|---|---|---|
| access_profiles | public | jsonb-list:permissions | permissions | PROMOTE-TO-CHILD: create <table>_permissions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| action_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| action_tracking | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| agent_credentials | tenant | array-column:scopes:TEXT[] | scopes | PROMOTE-TO-CHILD: create <table>_scopes (parent_id, scope) with FK; drop column scopes |  |
| agent_tool_permissions | tenant | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| agrc_os_cycle_log | tenant | jsonb-list:warnings | warnings | PROMOTE-TO-CHILD: create <table>_warnings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_agent_registry | public | jsonb-list:capabilities | capabilities | PROMOTE-TO-CHILD: create <table>_capabilities(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_governance_policies | tenant | jsonb-list:rules | rules | PROMOTE-TO-CHILD: create <table>_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_registry | public | jsonb-list:capabilities | capabilities | PROMOTE-TO-CHILD: create <table>_capabilities(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_registry | public | jsonb-list:performance_metrics | performance_metrics | PROMOTE-TO-CHILD: create <table>_performance_metrics(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_registry | public | jsonb-list:approved_use_cases | approved_use_cases | PROMOTE-TO-CHILD: create <table>_approved_use_cases(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_registry | public | jsonb-list:restricted_use_cases | restricted_use_cases | PROMOTE-TO-CHILD: create <table>_restricted_use_cases(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_risk_assessments | tenant | jsonb-list:risk_findings | risk_findings | PROMOTE-TO-CHILD: create <table>_risk_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_risk_assessments | tenant | jsonb-list:recommendations | recommendations | PROMOTE-TO-CHILD: create <table>_recommendations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_model_risk_scores | tenant | jsonb-list:risk_factors | risk_factors | PROMOTE-TO-CHILD: create <table>_risk_factors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_risk_assessments | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_risk_assessments | tenant | jsonb-list:mitigations | mitigations | PROMOTE-TO-CHILD: create <table>_mitigations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ai_suggestions | public | array-column:reason_codes:TEXT[] | reason_codes | PROMOTE-TO-CHILD: create <table>_reason_codes (parent_id, reason_code) with FK; drop column reason_codes |  |
| ai_suggestions | public | array-column:limitations:TEXT[] | limitations | PROMOTE-TO-CHILD: create <table>_limitations (parent_id, limitation) with FK; drop column limitations |  |
| ai_suggestions | public | jsonb-list:decision_factors | decision_factors | PROMOTE-TO-CHILD: create <table>_decision_factors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| analytics_benchmarks | tenant | repeating-group:percentile_*:3 | percentile_1..percentile_3 | PROMOTE-TO-CHILD: create <table>_percentiles(parent_id, position, value); drop percentile_N columns |  |
| analytics_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| analytics_scheduled_reports | tenant | jsonb-list:recipients | recipients | PROMOTE-TO-CHILD: create <table>_recipients(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| analytics_scheduled_reports | tenant | jsonb-list:filters | filters | PROMOTE-TO-CHILD: create <table>_filters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| analytics_snapshots | tenant | jsonb-list:metrics | metrics | PROMOTE-TO-CHILD: create <table>_metrics(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| api_keys | public | jsonb-list:scopes | scopes | PROMOTE-TO-CHILD: create <table>_scopes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| approval_chains | public | jsonb-list:steps | steps | PROMOTE-TO-CHILD: create <table>_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| as_built_ledger | public | jsonb-list:runtime_dependencies | runtime_dependencies | PROMOTE-TO-CHILD: create <table>_runtime_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| as_built_ledger | public | jsonb-list:known_limitations | known_limitations | PROMOTE-TO-CHILD: create <table>_known_limitations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| asset_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| attestation_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| audit_findings | public | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| audit_regulatory | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| audit_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| audit_risk_assessments | tenant | jsonb-list:risk_factors | risk_factors | PROMOTE-TO-CHILD: create <table>_risk_factors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| audit_trail | public | jsonb-list:changes | changes | PROMOTE-TO-CHILD: create <table>_changes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| autonomous_engine_log | tenant | jsonb-list:warnings | warnings | PROMOTE-TO-CHILD: create <table>_warnings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_bia_templates | tenant | jsonb-list:impact_categories | impact_categories | PROMOTE-TO-CHILD: create <table>_impact_categories(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_bia_templates | tenant | jsonb-list:time_frames | time_frames | PROMOTE-TO-CHILD: create <table>_time_frames(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_bia_templates | tenant | jsonb-list:questions | questions | PROMOTE-TO-CHILD: create <table>_questions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_crisis_teams | tenant | jsonb-list:members | members | PROMOTE-TO-CHILD: create <table>_members(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_exercise_schedule | tenant | jsonb-list:results | results | PROMOTE-TO-CHILD: create <table>_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_exercises | tenant | jsonb-list:participants | participants | PROMOTE-TO-CHILD: create <table>_participants(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_exercises | tenant | jsonb-list:objectives | objectives | PROMOTE-TO-CHILD: create <table>_objectives(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_exercises | tenant | jsonb-list:results | results | PROMOTE-TO-CHILD: create <table>_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_impact_analysis | tenant | jsonb-list:dependencies | dependencies | PROMOTE-TO-CHILD: create <table>_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_maturity_scores | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_recovery_strategies | tenant | jsonb-list:resource_requirements | resource_requirements | PROMOTE-TO-CHILD: create <table>_resource_requirements(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| bcp_scenarios | tenant | jsonb-list:recovery_procedures | recovery_procedures | PROMOTE-TO-CHILD: create <table>_recovery_procedures(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| benchmarks_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| breach_reporting_records | tenant | jsonb-list:data_categories | data_categories | PROMOTE-TO-CHILD: create <table>_data_categories(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| case_notes | tenant | jsonb-list:attachments | attachments | PROMOTE-TO-CHILD: create <table>_attachments(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_attestations | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_calendar | tenant | jsonb-list:reminders | reminders | PROMOTE-TO-CHILD: create <table>_reminders(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_gaps | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_regulatory_changes | tenant | jsonb-list:action_items | action_items | PROMOTE-TO-CHILD: create <table>_action_items(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| compliance_roadmap | tenant | jsonb-list:dependencies | dependencies | PROMOTE-TO-CHILD: create <table>_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| conditional_access_policies | public | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| config_definitions | public | array-column:allowed_scopes:TEXT[] | allowed_scopes | PROMOTE-TO-CHILD: create <table>_allowed_scopes (parent_id, allowed_scope) with FK; drop column allowed_scopes |  |
| config_definitions | public | array-column:enum_values:TEXT[] | enum_values | PROMOTE-TO-CHILD: create <table>_enum_values (parent_id, enum_value) with FK; drop column enum_values |  |
| content_packs | public | jsonb-list:modules | modules | PROMOTE-TO-CHILD: create <table>_modules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| content_packs | public | jsonb-list:seeds | seeds | PROMOTE-TO-CHILD: create <table>_seeds(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| contract_catalog | public | array-column:readers:TEXT[] | readers | PROMOTE-TO-CHILD: create <table>_readers (parent_id, reader) with FK; drop column readers |  |
| contract_catalog | public | array-column:writers:TEXT[] | writers | PROMOTE-TO-CHILD: create <table>_writers (parent_id, writer) with FK; drop column writers |  |
| control_tests | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| controls | public | jsonb-list:framework_refs | framework_refs | PROMOTE-TO-CHILD: create <table>_framework_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| controls | public | jsonb-list:risk_refs | risk_refs | PROMOTE-TO-CHILD: create <table>_risk_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| controls_certifications | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| controls_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| controls_testing | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| csa_campaigns | tenant | jsonb-list:control_ids | control_ids | PROMOTE-TO-CHILD: create <table>_control_ids(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| csa_campaigns | tenant | jsonb-list:respondent_ids | respondent_ids | PROMOTE-TO-CHILD: create <table>_respondent_ids(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dashboard_editor_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dashboard_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_assessments | public | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_ict_assets | tenant | jsonb-list:dependencies | dependencies | PROMOTE-TO-CHILD: create <table>_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_resilience_tests | tenant | jsonb-list:results | results | PROMOTE-TO-CHILD: create <table>_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_threat_intelligence | tenant | jsonb-list:affected_assets | affected_assets | PROMOTE-TO-CHILD: create <table>_affected_assets(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dora_threat_intelligence | tenant | jsonb-list:indicators | indicators | PROMOTE-TO-CHILD: create <table>_indicators(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_compatibility_records | public | jsonb-list:affected_consumers | affected_consumers | PROMOTE-TO-CHILD: create <table>_affected_consumers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_cutover_executions | public | jsonb-list:passed_checkpoints | passed_checkpoints | PROMOTE-TO-CHILD: create <table>_passed_checkpoints(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_cutover_plans | public | jsonb-list:rollback_triggers | rollback_triggers | PROMOTE-TO-CHILD: create <table>_rollback_triggers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_feature_gates | public | jsonb-list:allowed_roles | allowed_roles | PROMOTE-TO-CHILD: create <table>_allowed_roles(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_feature_gates | public | jsonb-list:allowed_tenants | allowed_tenants | PROMOTE-TO-CHILD: create <table>_allowed_tenants(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_migrations | public | jsonb-list:affected_schemas | affected_schemas | PROMOTE-TO-CHILD: create <table>_affected_schemas(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_migrations | public | jsonb-list:affected_tables | affected_tables | PROMOTE-TO-CHILD: create <table>_affected_tables(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_migrations | public | jsonb-list:validation_steps | validation_steps | PROMOTE-TO-CHILD: create <table>_validation_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_releases | public | jsonb-list:affected_layers | affected_layers | PROMOTE-TO-CHILD: create <table>_affected_layers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_releases | public | jsonb-list:affected_products | affected_products | PROMOTE-TO-CHILD: create <table>_affected_products(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_releases | public | jsonb-list:affected_modules | affected_modules | PROMOTE-TO-CHILD: create <table>_affected_modules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| dos_releases | public | jsonb-list:migrations | migrations | PROMOTE-TO-CHILD: create <table>_migrations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| editor_layouts | tenant | jsonb-list:breakpoints | breakpoints | PROMOTE-TO-CHILD: create <table>_breakpoints(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| enforcement_check_results | public | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| event_traces | public | jsonb-list:spans | spans | PROMOTE-TO-CHILD: create <table>_spans(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| evidence_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| evidence_vault | tenant | jsonb-list:file_refs | file_refs | PROMOTE-TO-CHILD: create <table>_file_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| exception_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| executive_briefings | tenant | jsonb-list:key_findings | key_findings | PROMOTE-TO-CHILD: create <table>_key_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| executive_briefings | tenant | jsonb-list:action_items | action_items | PROMOTE-TO-CHILD: create <table>_action_items(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| executive_dashboards | tenant | jsonb-list:sections | sections | PROMOTE-TO-CHILD: create <table>_sections(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| executive_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| fitch_assessments | tenant | jsonb-list:gap_analysis | gap_analysis | PROMOTE-TO-CHILD: create <table>_gap_analysis(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| fitch_assessments | tenant | jsonb-list:recommendations | recommendations | PROMOTE-TO-CHILD: create <table>_recommendations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| fitch_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| form_conditional_rules | tenant | array-column:target_fields:TEXT[] | target_fields | PROMOTE-TO-CHILD: create <table>_target_fields (parent_id, target_field) with FK; drop column target_fields |  |
| governance_ai_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_boards | tenant | jsonb-list:members | members | PROMOTE-TO-CHILD: create <table>_members(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_delegations | tenant | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_health_scores | tenant | jsonb-list:components | components | PROMOTE-TO-CHILD: create <table>_components(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_meetings | tenant | jsonb-list:attendees | attendees | PROMOTE-TO-CHILD: create <table>_attendees(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_os_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| governance_reviews | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| grc_query_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| inbox_bulk_actions | tenant | jsonb-list:item_ids | item_ids | PROMOTE-TO-CHILD: create <table>_item_ids(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| inbox_preferences | tenant | array-column:priority_filter:TEXT[] | priority_filter | PROMOTE-TO-CHILD: create <table>_priority_filter (parent_id, priority_filter) with FK; drop column priority_filter |  |
| inbox_preferences | tenant | jsonb-list:notification_channels | notification_channels | PROMOTE-TO-CHILD: create <table>_notification_channels(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| inbox_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_investigations | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_notification_templates | tenant | array-column:channels:TEXT[] | channels | PROMOTE-TO-CHILD: create <table>_channels (parent_id, channel) with FK; drop column channels |  |
| incident_pir | tenant | jsonb-list:root_causes | root_causes | PROMOTE-TO-CHILD: create <table>_root_causes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:contributing_factors | contributing_factors | PROMOTE-TO-CHILD: create <table>_contributing_factors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:impact_analysis | impact_analysis | PROMOTE-TO-CHILD: create <table>_impact_analysis(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:recommendations | recommendations | PROMOTE-TO-CHILD: create <table>_recommendations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:action_items | action_items | PROMOTE-TO-CHILD: create <table>_action_items(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:attendees | attendees | PROMOTE-TO-CHILD: create <table>_attendees(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_pir | tenant | jsonb-list:attachments | attachments | PROMOTE-TO-CHILD: create <table>_attachments(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_post_incident_reviews | tenant | jsonb-list:attendees | attendees | PROMOTE-TO-CHILD: create <table>_attendees(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_post_incident_reviews | tenant | jsonb-list:action_items | action_items | PROMOTE-TO-CHILD: create <table>_action_items(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_recurring_patterns | tenant | jsonb-list:affected_systems | affected_systems | PROMOTE-TO-CHILD: create <table>_affected_systems(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_recurring_patterns | tenant | jsonb-list:taxonomy_nodes | taxonomy_nodes | PROMOTE-TO-CHILD: create <table>_taxonomy_nodes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_regulatory_notifications | tenant | jsonb-list:follow_up_actions | follow_up_actions | PROMOTE-TO-CHILD: create <table>_follow_up_actions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_regulatory_notifications | tenant | jsonb-list:attachments | attachments | PROMOTE-TO-CHILD: create <table>_attachments(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_response_teams | tenant | array-column:notification_channels:TEXT[] | notification_channels | PROMOTE-TO-CHILD: create <table>_notification_channels (parent_id, notification_channel) with FK; drop column notification_channels |  |
| incident_response_teams | tenant | jsonb-list:members | members | PROMOTE-TO-CHILD: create <table>_members(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incident_timelines | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| incidents | public | jsonb-list:affected_systems | affected_systems | PROMOTE-TO-CHILD: create <table>_affected_systems(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| integrations | public | jsonb-list:credentials | credentials | PROMOTE-TO-CHILD: create <table>_credentials(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| integrations_api_keys | tenant | jsonb-list:scopes | scopes | PROMOTE-TO-CHILD: create <table>_scopes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| integrations_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| integrations_webhooks | tenant | jsonb-list:events | events | PROMOTE-TO-CHILD: create <table>_events(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| issues_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| journey_maturity_scores | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| journey_milestones | tenant | jsonb-list:dependencies | dependencies | PROMOTE-TO-CHILD: create <table>_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| journey_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| journey_roadmap_items | tenant | jsonb-list:dependencies | dependencies | PROMOTE-TO-CHILD: create <table>_dependencies(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| knowledge_articles | tenant | jsonb-list:locale_variants | locale_variants | PROMOTE-TO-CHILD: create <table>_locale_variants(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| knowledge_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ksa_regulatory_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| lifecycle_transitions | public | jsonb-list:guard_conditions | guard_conditions | PROMOTE-TO-CHILD: create <table>_guard_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| local_knowledge_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| lookup_cloud_providers | public | array-column:regions_available:TEXT[] | regions_available | PROMOTE-TO-CHILD: create <table>_regions_available (parent_id, regions_available) with FK; drop column regions_available |  |
| lookup_sectors | public | array-column:typical_frameworks:TEXT[] | typical_frameworks | PROMOTE-TO-CHILD: create <table>_typical_frameworks (parent_id, typical_framework) with FK; drop column typical_frameworks |  |
| lookup_team_functions | public | array-column:required_for_sectors:TEXT[] | required_for_sectors | PROMOTE-TO-CHILD: create <table>_required_for_sectors (parent_id, required_for_sector) with FK; drop column required_for_sectors |  |
| lookup_timezones | public | array-column:countries:TEXT[] | countries | PROMOTE-TO-CHILD: create <table>_countries (parent_id, countrie) with FK; drop column countries |  |
| mcp_agent_registry | public | array-column:module_codes:TEXT[] | module_codes | PROMOTE-TO-CHILD: create <table>_module_codes (parent_id, module_code) with FK; drop column module_codes |  |
| mcp_agent_registry | public | jsonb-list:guardrails | guardrails | PROMOTE-TO-CHILD: create <table>_guardrails(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| mcp_prompt_registry | public | jsonb-list:guardrails | guardrails | PROMOTE-TO-CHILD: create <table>_guardrails(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| mcp_prompts | tenant | jsonb-list:arguments | arguments | PROMOTE-TO-CHILD: create <table>_arguments(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| mcp_resource_registry | public | array-column:requires_permission:TEXT[] | requires_permission | PROMOTE-TO-CHILD: create <table>_requires_permission (parent_id, requires_permission) with FK; drop column requires_permission |  |
| mcp_servers | tenant | jsonb-list:capabilities | capabilities | PROMOTE-TO-CHILD: create <table>_capabilities(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| mcp_tool_registry | public | array-column:required_permissions:TEXT[] | required_permissions | PROMOTE-TO-CHILD: create <table>_required_permissions (parent_id, required_permission) with FK; drop column required_permissions |  |
| mcp_tool_registry | public | array-column:required_roles:TEXT[] | required_roles | PROMOTE-TO-CHILD: create <table>_required_roles (parent_id, required_role) with FK; drop column required_roles |  |
| mcp_tool_registry | public | array-column:allowed_actor_types:TEXT[] | allowed_actor_types | PROMOTE-TO-CHILD: create <table>_allowed_actor_types (parent_id, allowed_actor_type) with FK; drop column allowed_actor_types |  |
| mcp_tool_registry | public | array-column:allowed_platform_modes:TEXT[] | allowed_platform_modes | PROMOTE-TO-CHILD: create <table>_allowed_platform_modes (parent_id, allowed_platform_mode) with FK; drop column allowed_platform_modes |  |
| mobile_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| module_sod_rules | public | array-column:escalation_roles:TEXT[] | escalation_roles | PROMOTE-TO-CHILD: create <table>_escalation_roles (parent_id, escalation_role) with FK; drop column escalation_roles |  |
| module_visibility_contracts | public | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| near_miss_reports | tenant | jsonb-list:attachments | attachments | PROMOTE-TO-CHILD: create <table>_attachments(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| notification_digests | tenant | jsonb-list:preferences | preferences | PROMOTE-TO-CHILD: create <table>_preferences(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| notification_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| notification_rules | tenant | jsonb-list:channels | channels | PROMOTE-TO-CHILD: create <table>_channels(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| notification_templates | tenant | jsonb-list:variables | variables | PROMOTE-TO-CHILD: create <table>_variables(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_answer_analytics | public | jsonb-list:validation_errors | validation_errors | PROMOTE-TO-CHILD: create <table>_validation_errors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_configs | public | jsonb-list:steps | steps | PROMOTE-TO-CHILD: create <table>_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_configs | public | jsonb-list:completed_steps | completed_steps | PROMOTE-TO-CHILD: create <table>_completed_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_inference_bundles | public | array-column:deeper_question_codes:TEXT[] | deeper_question_codes | PROMOTE-TO-CHILD: create <table>_deeper_question_codes (parent_id, deeper_question_code) with FK; drop column deeper_question_codes |  |
| onboarding_provisioning_log | tenant | jsonb-list:details | details | PROMOTE-TO-CHILD: create <table>_details(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_question_audit | public | jsonb-list:old_values | old_values | PROMOTE-TO-CHILD: create <table>_old_values(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_question_audit | public | jsonb-list:new_values | new_values | PROMOTE-TO-CHILD: create <table>_new_values(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_question_bank | public | jsonb-list:signals | signals | PROMOTE-TO-CHILD: create <table>_signals(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_session_answers | public | jsonb-list:validation_errors | validation_errors | PROMOTE-TO-CHILD: create <table>_validation_errors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_session_progress | public | array-column:stages_completed:TEXT[] | stages_completed | PROMOTE-TO-CHILD: create <table>_stages_completed (parent_id, stages_completed) with FK; drop column stages_completed |  |
| onboarding_sessions | public | array-column:completed_stages:VARCHAR[] | completed_stages | PROMOTE-TO-CHILD: create <table>_completed_stages (parent_id, completed_stage) with FK; drop column completed_stages |  |
| onboarding_sessions | public | jsonb-list:stage_scores | stage_scores | PROMOTE-TO-CHILD: create <table>_stage_scores(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_sessions | public | jsonb-list:category_scores | category_scores | PROMOTE-TO-CHILD: create <table>_category_scores(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_stage_definitions | public | jsonb-list:validation_rules | validation_rules | PROMOTE-TO-CHILD: create <table>_validation_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| onboarding_workspace_config | tenant | array-column:enabled_modules:TEXT[] | enabled_modules | PROMOTE-TO-CHILD: create <table>_enabled_modules (parent_id, enabled_module) with FK; drop column enabled_modules |  |
| onboarding_workspace_config | tenant | array-column:enabled_frameworks:TEXT[] | enabled_frameworks | PROMOTE-TO-CHILD: create <table>_enabled_frameworks (parent_id, enabled_framework) with FK; drop column enabled_frameworks |  |
| onboarding_workspace_handoff_cycles | public | array-column:agents_planned:TEXT[] | agents_planned | PROMOTE-TO-CHILD: create <table>_agents_planned (parent_id, agents_planned) with FK; drop column agents_planned |  |
| onboarding_workspace_handoff_cycles | public | array-column:agents_dispatched:TEXT[] | agents_dispatched | PROMOTE-TO-CHILD: create <table>_agents_dispatched (parent_id, agents_dispatched) with FK; drop column agents_dispatched |  |
| operating_cockpit_dashboards | tenant | jsonb-list:widgets | widgets | PROMOTE-TO-CHILD: create <table>_widgets(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| operating_cockpit_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| os_configurations | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| os_health_checks | tenant | jsonb-list:details | details | PROMOTE-TO-CHILD: create <table>_details(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| os_runbooks | tenant | jsonb-list:steps | steps | PROMOTE-TO-CHILD: create <table>_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| os_runbooks | tenant | jsonb-list:rollback_steps | rollback_steps | PROMOTE-TO-CHILD: create <table>_rollback_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| permission_cache | tenant | jsonb-list:permissions | permissions | PROMOTE-TO-CHILD: create <table>_permissions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| permission_cache | tenant | jsonb-list:modules | modules | PROMOTE-TO-CHILD: create <table>_modules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| permission_cache | tenant | jsonb-list:roles | roles | PROMOTE-TO-CHILD: create <table>_roles(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| platform_outbox | public | jsonb-list:headers | headers | PROMOTE-TO-CHILD: create <table>_headers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| platform_outbox_archive | public | jsonb-list:headers | headers | PROMOTE-TO-CHILD: create <table>_headers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| platform_products | public | array-column:domains:TEXT[] | domains | PROMOTE-TO-CHILD: create <table>_domains (parent_id, domain) with FK; drop column domains |  |
| playbooks_definitions | tenant | jsonb-list:steps | steps | PROMOTE-TO-CHILD: create <table>_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| playbooks_definitions | tenant | jsonb-list:escalation_rules | escalation_rules | PROMOTE-TO-CHILD: create <table>_escalation_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| playbooks_executions | tenant | jsonb-list:step_results | step_results | PROMOTE-TO-CHILD: create <table>_step_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| playbooks_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| policies | public | jsonb-list:framework_refs | framework_refs | PROMOTE-TO-CHILD: create <table>_framework_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| policy_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| portals_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_breach_register | tenant | jsonb-list:data_categories | data_categories | PROMOTE-TO-CHILD: create <table>_data_categories(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_impact_assessments | tenant | jsonb-list:recommendations | recommendations | PROMOTE-TO-CHILD: create <table>_recommendations(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_processing_activities | tenant | jsonb-list:data_categories | data_categories | PROMOTE-TO-CHILD: create <table>_data_categories(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_processing_activities | tenant | jsonb-list:data_subjects | data_subjects | PROMOTE-TO-CHILD: create <table>_data_subjects(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_processing_activities | tenant | jsonb-list:recipients | recipients | PROMOTE-TO-CHILD: create <table>_recipients(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| privacy_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| proactive_leadership_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| provisioning_step_definitions | public | array-column:depends_on:TEXT[] | depends_on | PROMOTE-TO-CHILD: create <table>_depends_on (parent_id, depends_on) with FK; drop column depends_on |  |
| qiyas_assessment_responses | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| qiyas_improvement_plans | tenant | jsonb-list:actions | actions | PROMOTE-TO-CHILD: create <table>_actions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| qiyas_journeys | public | jsonb-list:results | results | PROMOTE-TO-CHILD: create <table>_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| qiyas_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| query_history | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| query_templates | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| query_templates | tenant | jsonb-list:target_modules | target_modules | PROMOTE-TO-CHILD: create <table>_target_modules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| question_bank | public | jsonb-list:options | options | PROMOTE-TO-CHILD: create <table>_options(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| question_bank | public | jsonb-list:validation_rules | validation_rules | PROMOTE-TO-CHILD: create <table>_validation_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| raci_matrices | tenant | jsonb-list:roles | roles | PROMOTE-TO-CHILD: create <table>_roles(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| records_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| regulatory_gap_analysis | tenant | jsonb-list:gaps | gaps | PROMOTE-TO-CHILD: create <table>_gaps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| regulatory_updates | tenant | jsonb-list:affected_areas | affected_areas | PROMOTE-TO-CHILD: create <table>_affected_areas(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| remediation_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| remediation_root_causes | tenant | jsonb-list:contributing_factors | contributing_factors | PROMOTE-TO-CHILD: create <table>_contributing_factors(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| report_schedules | public | jsonb-list:recipients | recipients | PROMOTE-TO-CHILD: create <table>_recipients(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_data_extracts | tenant | jsonb-list:filters | filters | PROMOTE-TO-CHILD: create <table>_filters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_generated | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_scheduled | tenant | jsonb-list:recipients | recipients | PROMOTE-TO-CHILD: create <table>_recipients(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_scheduled | tenant | jsonb-list:filters | filters | PROMOTE-TO-CHILD: create <table>_filters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| reporting_templates | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risk_approval_requests | tenant | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risk_reviews | tenant | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risk_scenarios | tenant | jsonb-list:assumptions | assumptions | PROMOTE-TO-CHILD: create <table>_assumptions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risk_scenarios | tenant | jsonb-list:treatment_options | treatment_options | PROMOTE-TO-CHILD: create <table>_treatment_options(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risk_score_history | tenant | jsonb-list:dimension_scores | dimension_scores | PROMOTE-TO-CHILD: create <table>_dimension_scores(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risks | public | jsonb-list:entity_links | entity_links | PROMOTE-TO-CHILD: create <table>_entity_links(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| risks | public | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| role_profiles | tenant | jsonb-list:modules | modules | PROMOTE-TO-CHILD: create <table>_modules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| role_profiles | tenant | jsonb-list:dashboard_widgets | dashboard_widgets | PROMOTE-TO-CHILD: create <table>_dashboard_widgets(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:trigger_types | trigger_types | PROMOTE-TO-CHILD: create <table>_trigger_types(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:linked_alert_ids | linked_alert_ids | PROMOTE-TO-CHILD: create <table>_linked_alert_ids(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:health_signals | health_signals | PROMOTE-TO-CHILD: create <table>_health_signals(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:dashboard_links | dashboard_links | PROMOTE-TO-CHILD: create <table>_dashboard_links(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:alert_sources | alert_sources | PROMOTE-TO-CHILD: create <table>_alert_sources(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:recovery_steps | recovery_steps | PROMOTE-TO-CHILD: create <table>_recovery_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:rollback_steps | rollback_steps | PROMOTE-TO-CHILD: create <table>_rollback_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:common_failure_modes | common_failure_modes | PROMOTE-TO-CHILD: create <table>_common_failure_modes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:known_caveats | known_caveats | PROMOTE-TO-CHILD: create <table>_known_caveats(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| runbook_definitions | public | jsonb-list:support_contacts | support_contacts | PROMOTE-TO-CHILD: create <table>_support_contacts(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| shell_extensions | public | jsonb-list:guard_conditions | guard_conditions | PROMOTE-TO-CHILD: create <table>_guard_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| sod_conflict_audit_template | public | array-column:role_codes:TEXT[] | role_codes | PROMOTE-TO-CHILD: create <table>_role_codes (parent_id, role_code) with FK; drop column role_codes |  |
| sod_conflict_audit_template | public | jsonb-list:conflicting_rules | conflicting_rules | PROMOTE-TO-CHILD: create <table>_conflicting_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| sso_identities | public | jsonb-list:attributes | attributes | PROMOTE-TO-CHILD: create <table>_attributes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| sso_sessions | public | jsonb-list:attributes | attributes | PROMOTE-TO-CHILD: create <table>_attributes(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| stage_definitions | public | jsonb-list:validation_rules | validation_rules | PROMOTE-TO-CHILD: create <table>_validation_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| team_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| templates | public | array-column:recommended_modules:TEXT[] | recommended_modules | PROMOTE-TO-CHILD: create <table>_recommended_modules (parent_id, recommended_module) with FK; drop column recommended_modules |  |
| templates | public | jsonb-list:pre_filled_answers | pre_filled_answers | PROMOTE-TO-CHILD: create <table>_pre_filled_answers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| tenant_boundary_configs | public | array-column:cross_tenant_partners:TEXT[] | cross_tenant_partners | PROMOTE-TO-CHILD: create <table>_cross_tenant_partners (parent_id, cross_tenant_partner) with FK; drop column cross_tenant_partners |  |
| tenant_module_entitlements | public | array-column:licensed_modules:TEXT[] | licensed_modules | PROMOTE-TO-CHILD: create <table>_licensed_modules (parent_id, licensed_module) with FK; drop column licensed_modules |  |
| tenants | public | array-column:regions:TEXT[] | regions | PROMOTE-TO-CHILD: create <table>_regions (parent_id, region) with FK; drop column regions |  |
| tenants | public | array-column:active_frameworks:TEXT[] | active_frameworks | PROMOTE-TO-CHILD: create <table>_active_frameworks (parent_id, active_framework) with FK; drop column active_frameworks |  |
| tenants | public | array-column:operating_regions:TEXT[] | operating_regions | PROMOTE-TO-CHILD: create <table>_operating_regions (parent_id, operating_region) with FK; drop column operating_regions |  |
| tenants | public | array-column:cross_border_countries:TEXT[] | cross_border_countries | PROMOTE-TO-CHILD: create <table>_cross_border_countries (parent_id, cross_border_countrie) with FK; drop column cross_border_countries |  |
| tenants | public | array-column:sector_ids:TEXT[] | sector_ids | PROMOTE-TO-CHILD: create <table>_sector_ids (parent_id, sector_id) with FK; drop column sector_ids |  |
| tenants | public | array-column:cloud_providers:TEXT[] | cloud_providers | PROMOTE-TO-CHILD: create <table>_cloud_providers (parent_id, cloud_provider) with FK; drop column cloud_providers |  |
| tenants | public | array-column:existing_certifications:TEXT[] | existing_certifications | PROMOTE-TO-CHILD: create <table>_existing_certifications (parent_id, existing_certification) with FK; drop column existing_certifications |  |
| tenants | public | array-column:target_certifications:TEXT[] | target_certifications | PROMOTE-TO-CHILD: create <table>_target_certifications (parent_id, target_certification) with FK; drop column target_certifications |  |
| tenants | public | array-column:regulator_ids:TEXT[] | regulator_ids | PROMOTE-TO-CHILD: create <table>_regulator_ids (parent_id, regulator_id) with FK; drop column regulator_ids |  |
| tier_definitions | public | array-column:features:TEXT[] | features | PROMOTE-TO-CHILD: create <table>_features (parent_id, feature) with FK; drop column features |  |
| tier_definitions | public | jsonb-list:limits | limits | PROMOTE-TO-CHILD: create <table>_limits(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| training_campaigns | tenant | jsonb-list:courses | courses | PROMOTE-TO-CHILD: create <table>_courses(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| training_phishing_simulations | tenant | jsonb-list:target_users | target_users | PROMOTE-TO-CHILD: create <table>_target_users(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| training_phishing_simulations | tenant | jsonb-list:results | results | PROMOTE-TO-CHILD: create <table>_results(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| training_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ucf_controls | tenant | jsonb-list:evidence_requirements | evidence_requirements | PROMOTE-TO-CHILD: create <table>_evidence_requirements(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ucf_controls | tenant | jsonb-list:test_steps | test_steps | PROMOTE-TO-CHILD: create <table>_test_steps(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| ucf_controls | tenant | jsonb-list:exception_rules | exception_rules | PROMOTE-TO-CHILD: create <table>_exception_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| user_preferences | tenant | jsonb-list:preferences | preferences | PROMOTE-TO-CHILD: create <table>_preferences(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| users | public | array-column:certifications:TEXT[] | certifications | PROMOTE-TO-CHILD: create <table>_certifications (parent_id, certification) with FK; drop column certifications |  |
| users | public | jsonb-list:notification_prefs | notification_prefs | PROMOTE-TO-CHILD: create <table>_notification_prefs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_assessments | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_due_diligence | tenant | jsonb-list:findings | findings | PROMOTE-TO-CHILD: create <table>_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_due_diligence_checklists | tenant | array-column:tier_codes:TEXT[] | tier_codes | PROMOTE-TO-CHILD: create <table>_tier_codes (parent_id, tier_code) with FK; drop column tier_codes |  |
| vendor_due_diligence_checklists | tenant | jsonb-list:items | items | PROMOTE-TO-CHILD: create <table>_items(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_due_diligence_ext | tenant | jsonb-list:evidence_refs | evidence_refs | PROMOTE-TO-CHILD: create <table>_evidence_refs(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_due_diligence_ext | tenant | jsonb-list:risk_findings | risk_findings | PROMOTE-TO-CHILD: create <table>_risk_findings(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_monitoring | tenant | jsonb-list:alerts | alerts | PROMOTE-TO-CHILD: create <table>_alerts(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_questionnaire_templates | tenant | array-column:tier_codes:TEXT[] | tier_codes | PROMOTE-TO-CHILD: create <table>_tier_codes (parent_id, tier_code) with FK; drop column tier_codes |  |
| vendor_questionnaire_templates | tenant | jsonb-list:questions | questions | PROMOTE-TO-CHILD: create <table>_questions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| vendor_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| widgets_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| workflow_approvals | tenant | jsonb-list:required_approvers | required_approvers | PROMOTE-TO-CHILD: create <table>_required_approvers(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| workflow_delegation_rules | tenant | jsonb-list:conditions | conditions | PROMOTE-TO-CHILD: create <table>_conditions(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| workflow_report_snapshots | tenant | jsonb-list:parameters | parameters | PROMOTE-TO-CHILD: create <table>_parameters(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
| workflow_sla_configs | public | jsonb-list:escalation_rules | escalation_rules | PROMOTE-TO-CHILD: create <table>_escalation_rules(parent_id, seq, payload) or typed columns per sample shape (see jsonb-decompose-plan.ts) |  |
