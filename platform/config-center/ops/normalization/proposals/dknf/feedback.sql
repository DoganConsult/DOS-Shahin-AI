-- DKNF ENUM promotions for feedback
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── feedback.feedback_type (3 values) ───
-- CREATE TYPE public.feedback_feedback_type_enum AS ENUM ('nps', 'csat', 'ces');
-- ALTER TABLE public.feedback
--   ALTER COLUMN feedback_type TYPE public.feedback_feedback_type_enum
--   USING feedback_type::public.feedback_feedback_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.feedback'::regclass AND contype='c';
