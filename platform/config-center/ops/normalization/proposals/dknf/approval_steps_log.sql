-- DKNF ENUM promotions for approval_steps_log
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── approval_steps_log.action (5 values) ───
-- CREATE TYPE public.approval_steps_log_action_enum AS ENUM ('submitted', 'approved', 'rejected', 'delegated', 'escalated');
-- ALTER TABLE public.approval_steps_log
--   ALTER COLUMN action TYPE public.approval_steps_log_action_enum
--   USING action::public.approval_steps_log_action_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.approval_steps_log'::regclass AND contype='c';
