-- DKNF ENUM promotions for issues_register
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── issues_register.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.issues_register_status_enum AS ENUM ('open', 'in_progress', 'resolved', 'closed', 'deferred');
-- ALTER TABLE __TENANT_SCHEMA__.issues_register
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.issues_register_status_enum
--   USING status::__TENANT_SCHEMA__.issues_register_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.issues_register'::regclass AND contype='c';
