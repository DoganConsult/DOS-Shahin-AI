-- DKNF ENUM promotions for integrations_sync_log
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── integrations_sync_log.direction (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.integrations_sync_log_direction_enum AS ENUM ('inbound', 'outbound', 'bidirectional');
-- ALTER TABLE __TENANT_SCHEMA__.integrations_sync_log
--   ALTER COLUMN direction TYPE __TENANT_SCHEMA__.integrations_sync_log_direction_enum
--   USING direction::__TENANT_SCHEMA__.integrations_sync_log_direction_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.integrations_sync_log'::regclass AND contype='c';
