-- DKNF ENUM promotions for lookup_tra_licenses
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_tra_licenses.service_type (4 values) ───
-- CREATE TYPE public.lookup_tra_licenses_service_type_enum AS ENUM ('mobile', 'fixed', 'internet', 'data_center');
-- ALTER TABLE public.lookup_tra_licenses
--   ALTER COLUMN service_type TYPE public.lookup_tra_licenses_service_type_enum
--   USING service_type::public.lookup_tra_licenses_service_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_tra_licenses'::regclass AND contype='c';
