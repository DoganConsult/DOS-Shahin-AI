-- DKNF ENUM promotions for bcp_exercise_schedule
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── bcp_exercise_schedule.exercise_type (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.bcp_exercise_schedule_exercise_type_enum AS ENUM ('tabletop', 'walkthrough', 'simulation', 'full_test', 'notification_drill');
-- ALTER TABLE __TENANT_SCHEMA__.bcp_exercise_schedule
--   ALTER COLUMN exercise_type TYPE __TENANT_SCHEMA__.bcp_exercise_schedule_exercise_type_enum
--   USING exercise_type::__TENANT_SCHEMA__.bcp_exercise_schedule_exercise_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.bcp_exercise_schedule'::regclass AND contype='c';

-- ─── bcp_exercise_schedule.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.bcp_exercise_schedule_status_enum AS ENUM ('scheduled', 'in_progress', 'completed', 'cancelled', 'postponed');
-- ALTER TABLE __TENANT_SCHEMA__.bcp_exercise_schedule
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.bcp_exercise_schedule_status_enum
--   USING status::__TENANT_SCHEMA__.bcp_exercise_schedule_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.bcp_exercise_schedule'::regclass AND contype='c';
