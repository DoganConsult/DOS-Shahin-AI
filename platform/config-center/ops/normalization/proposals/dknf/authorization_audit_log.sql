-- DKNF ENUM promotions for authorization_audit_log
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── authorization_audit_log.decision (2 values) ───
-- CREATE TYPE public.authorization_audit_log_decision_enum AS ENUM ('allow', 'deny');
-- ALTER TABLE public.authorization_audit_log
--   ALTER COLUMN decision TYPE public.authorization_audit_log_decision_enum
--   USING decision::public.authorization_audit_log_decision_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.authorization_audit_log'::regclass AND contype='c';
