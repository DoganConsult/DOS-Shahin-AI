-- DKNF ENUM promotions for mcp_resource_registry
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mcp_resource_registry.sensitivity_level (4 values) ───
-- CREATE TYPE public.mcp_resource_registry_sensitivity_level_enum AS ENUM ('normal', 'elevated', 'high', 'critical');
-- ALTER TABLE public.mcp_resource_registry
--   ALTER COLUMN sensitivity_level TYPE public.mcp_resource_registry_sensitivity_level_enum
--   USING sensitivity_level::public.mcp_resource_registry_sensitivity_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_resource_registry'::regclass AND contype='c';

-- ─── mcp_resource_registry.data_classification (4 values) ───
-- CREATE TYPE public.mcp_resource_registry_data_classification_enum AS ENUM ('public', 'internal', 'confidential', 'restricted');
-- ALTER TABLE public.mcp_resource_registry
--   ALTER COLUMN data_classification TYPE public.mcp_resource_registry_data_classification_enum
--   USING data_classification::public.mcp_resource_registry_data_classification_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_resource_registry'::regclass AND contype='c';

-- ─── mcp_resource_registry.visibility_scope (4 values) ───
-- CREATE TYPE public.mcp_resource_registry_visibility_scope_enum AS ENUM ('all', 'internal', 'admin_only', 'beta');
-- ALTER TABLE public.mcp_resource_registry
--   ALTER COLUMN visibility_scope TYPE public.mcp_resource_registry_visibility_scope_enum
--   USING visibility_scope::public.mcp_resource_registry_visibility_scope_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_resource_registry'::regclass AND contype='c';

-- ─── mcp_resource_registry.status (4 values) ───
-- CREATE TYPE public.mcp_resource_registry_status_enum AS ENUM ('draft', 'active', 'deprecated', 'retired');
-- ALTER TABLE public.mcp_resource_registry
--   ALTER COLUMN status TYPE public.mcp_resource_registry_status_enum
--   USING status::public.mcp_resource_registry_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_resource_registry'::regclass AND contype='c';
