-- DKNF ENUM promotions for enforcement_check_results
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── enforcement_check_results.status (3 values) ───
-- CREATE TYPE public.enforcement_check_results_status_enum AS ENUM ('PASS', 'CONDITIONAL_PASS', 'FAIL');
-- ALTER TABLE public.enforcement_check_results
--   ALTER COLUMN status TYPE public.enforcement_check_results_status_enum
--   USING status::public.enforcement_check_results_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.enforcement_check_results'::regclass AND contype='c';
