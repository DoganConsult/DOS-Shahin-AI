-- DKNF ENUM promotions for startup_checklist
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── startup_checklist.priority (4 values) ───
-- CREATE TYPE public.startup_checklist_priority_enum AS ENUM ('critical', 'high', 'medium', 'low');
-- ALTER TABLE public.startup_checklist
--   ALTER COLUMN priority TYPE public.startup_checklist_priority_enum
--   USING priority::public.startup_checklist_priority_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.startup_checklist'::regclass AND contype='c';
