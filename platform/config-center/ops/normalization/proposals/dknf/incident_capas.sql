-- DKNF ENUM promotions for incident_capas
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── incident_capas.action_type (2 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_capas_action_type_enum AS ENUM ('corrective', 'preventive');
-- ALTER TABLE __TENANT_SCHEMA__.incident_capas
--   ALTER COLUMN action_type TYPE __TENANT_SCHEMA__.incident_capas_action_type_enum
--   USING action_type::__TENANT_SCHEMA__.incident_capas_action_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_capas'::regclass AND contype='c';

-- ─── incident_capas.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_capas_status_enum AS ENUM ('open', 'in_progress', 'completed', 'verified');
-- ALTER TABLE __TENANT_SCHEMA__.incident_capas
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.incident_capas_status_enum
--   USING status::__TENANT_SCHEMA__.incident_capas_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_capas'::regclass AND contype='c';
