-- DKNF ENUM promotions for provisioning_steps
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── provisioning_steps.status (4 values) ───
-- CREATE TYPE public.provisioning_steps_status_enum AS ENUM ('queued', 'running', 'completed', 'failed');
-- ALTER TABLE public.provisioning_steps
--   ALTER COLUMN status TYPE public.provisioning_steps_status_enum
--   USING status::public.provisioning_steps_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.provisioning_steps'::regclass AND contype='c';
