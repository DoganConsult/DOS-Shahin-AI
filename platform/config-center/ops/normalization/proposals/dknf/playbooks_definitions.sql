-- DKNF ENUM promotions for playbooks_definitions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── playbooks_definitions.trigger_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.playbooks_definitions_trigger_type_enum AS ENUM ('manual', 'event', 'scheduled', 'condition');
-- ALTER TABLE __TENANT_SCHEMA__.playbooks_definitions
--   ALTER COLUMN trigger_type TYPE __TENANT_SCHEMA__.playbooks_definitions_trigger_type_enum
--   USING trigger_type::__TENANT_SCHEMA__.playbooks_definitions_trigger_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.playbooks_definitions'::regclass AND contype='c';
