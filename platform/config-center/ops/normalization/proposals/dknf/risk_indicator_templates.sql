-- DKNF ENUM promotions for risk_indicator_templates
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── risk_indicator_templates.indicator_type (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_indicator_templates_indicator_type_enum AS ENUM ('kri', 'kci', 'kpi');
-- ALTER TABLE __TENANT_SCHEMA__.risk_indicator_templates
--   ALTER COLUMN indicator_type TYPE __TENANT_SCHEMA__.risk_indicator_templates_indicator_type_enum
--   USING indicator_type::__TENANT_SCHEMA__.risk_indicator_templates_indicator_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_indicator_templates'::regclass AND contype='c';
