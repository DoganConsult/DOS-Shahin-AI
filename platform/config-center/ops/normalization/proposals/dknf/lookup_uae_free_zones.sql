-- DKNF ENUM promotions for lookup_uae_free_zones
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_uae_free_zones.emirate (7 values) ───
-- CREATE TYPE public.lookup_uae_free_zones_emirate_enum AS ENUM ('dubai', 'abu_dhabi', 'sharjah', 'ajman', 'ras_al_khaimah', 'fujairah', 'umm_al_quwain');
-- ALTER TABLE public.lookup_uae_free_zones
--   ALTER COLUMN emirate TYPE public.lookup_uae_free_zones_emirate_enum
--   USING emirate::public.lookup_uae_free_zones_emirate_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_uae_free_zones'::regclass AND contype='c';
