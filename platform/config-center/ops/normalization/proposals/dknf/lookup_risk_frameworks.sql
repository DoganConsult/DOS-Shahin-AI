-- DKNF ENUM promotions for lookup_risk_frameworks
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_risk_frameworks.framework_type (4 values) ───
-- CREATE TYPE public.lookup_risk_frameworks_framework_type_enum AS ENUM ('enterprise', 'it', 'operational', 'financial');
-- ALTER TABLE public.lookup_risk_frameworks
--   ALTER COLUMN framework_type TYPE public.lookup_risk_frameworks_framework_type_enum
--   USING framework_type::public.lookup_risk_frameworks_framework_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_risk_frameworks'::regclass AND contype='c';
