-- DKNF ENUM promotions for slo_definitions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── slo_definitions.comparison_operator (4 values) ───
-- CREATE TYPE public.slo_definitions_comparison_operator_enum AS ENUM ('lt', 'lte', 'gt', 'gte');
-- ALTER TABLE public.slo_definitions
--   ALTER COLUMN comparison_operator TYPE public.slo_definitions_comparison_operator_enum
--   USING comparison_operator::public.slo_definitions_comparison_operator_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.slo_definitions'::regclass AND contype='c';
