-- DKNF ENUM promotions for event_dead_letter_queue
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── event_dead_letter_queue.status (4 values) ───
-- CREATE TYPE public.event_dead_letter_queue_status_enum AS ENUM ('failed', 'retrying', 'resolved', 'expired');
-- ALTER TABLE public.event_dead_letter_queue
--   ALTER COLUMN status TYPE public.event_dead_letter_queue_status_enum
--   USING status::public.event_dead_letter_queue_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.event_dead_letter_queue'::regclass AND contype='c';
