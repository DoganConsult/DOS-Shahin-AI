-- DKNF ENUM promotions for privacy_dsar_requests
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── privacy_dsar_requests.request_type (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.privacy_dsar_requests_request_type_enum AS ENUM ('access', 'rectification', 'erasure', 'portability', 'restriction', 'objection');
-- ALTER TABLE __TENANT_SCHEMA__.privacy_dsar_requests
--   ALTER COLUMN request_type TYPE __TENANT_SCHEMA__.privacy_dsar_requests_request_type_enum
--   USING request_type::__TENANT_SCHEMA__.privacy_dsar_requests_request_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.privacy_dsar_requests'::regclass AND contype='c';
