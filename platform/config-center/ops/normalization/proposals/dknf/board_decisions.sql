-- DKNF ENUM promotions for board_decisions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── board_decisions.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.board_decisions_status_enum AS ENUM ('draft', 'under_review', 'approved', 'rejected', 'deferred');
-- ALTER TABLE __TENANT_SCHEMA__.board_decisions
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.board_decisions_status_enum
--   USING status::__TENANT_SCHEMA__.board_decisions_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.board_decisions'::regclass AND contype='c';
