-- DKNF ENUM promotions for os_health_checks
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── os_health_checks.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.os_health_checks_status_enum AS ENUM ('passing', 'warning', 'failing', 'unknown');
-- ALTER TABLE __TENANT_SCHEMA__.os_health_checks
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.os_health_checks_status_enum
--   USING status::__TENANT_SCHEMA__.os_health_checks_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.os_health_checks'::regclass AND contype='c';
