-- DKNF ENUM promotions for blockers
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── blockers.severity (5 values) ───
-- CREATE TYPE public.blockers_severity_enum AS ENUM ('info', 'low', 'medium', 'high', 'critical');
-- ALTER TABLE public.blockers
--   ALTER COLUMN severity TYPE public.blockers_severity_enum
--   USING severity::public.blockers_severity_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.blockers'::regclass AND contype='c';
