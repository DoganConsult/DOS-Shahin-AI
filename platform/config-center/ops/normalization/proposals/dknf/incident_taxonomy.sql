-- DKNF ENUM promotions for incident_taxonomy
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── incident_taxonomy.node_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_taxonomy_node_type_enum AS ENUM ('root', 'category', 'subcategory', 'type');
-- ALTER TABLE __TENANT_SCHEMA__.incident_taxonomy
--   ALTER COLUMN node_type TYPE __TENANT_SCHEMA__.incident_taxonomy_node_type_enum
--   USING node_type::__TENANT_SCHEMA__.incident_taxonomy_node_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_taxonomy'::regclass AND contype='c';

-- ─── incident_taxonomy.severity_hint (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_taxonomy_severity_hint_enum AS ENUM ('low', 'medium', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.incident_taxonomy
--   ALTER COLUMN severity_hint TYPE __TENANT_SCHEMA__.incident_taxonomy_severity_hint_enum
--   USING severity_hint::__TENANT_SCHEMA__.incident_taxonomy_severity_hint_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_taxonomy'::regclass AND contype='c';
