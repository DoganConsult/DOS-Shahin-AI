-- DKNF ENUM promotions for mcp_tool_registry
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mcp_tool_registry.visibility_scope (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_visibility_scope_enum AS ENUM ('all', 'internal', 'admin_only', 'beta');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN visibility_scope TYPE public.mcp_tool_registry_visibility_scope_enum
--   USING visibility_scope::public.mcp_tool_registry_visibility_scope_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.execution_type (6 values) ───
-- CREATE TYPE public.mcp_tool_registry_execution_type_enum AS ENUM ('\n      ''internal_service', 'workflow_action', 'connector_action', '\n      ''http_proxy', 'job_dispatch', 'approval_only''\n');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN execution_type TYPE public.mcp_tool_registry_execution_type_enum
--   USING execution_type::public.mcp_tool_registry_execution_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.risk_level (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_risk_level_enum AS ENUM ('critical', 'high', 'medium', 'low');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN risk_level TYPE public.mcp_tool_registry_risk_level_enum
--   USING risk_level::public.mcp_tool_registry_risk_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.data_classification (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_data_classification_enum AS ENUM ('public', 'internal', 'confidential', 'restricted');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN data_classification TYPE public.mcp_tool_registry_data_classification_enum
--   USING data_classification::public.mcp_tool_registry_data_classification_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.sensitivity_level (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_sensitivity_level_enum AS ENUM ('normal', 'elevated', 'high', 'critical');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN sensitivity_level TYPE public.mcp_tool_registry_sensitivity_level_enum
--   USING sensitivity_level::public.mcp_tool_registry_sensitivity_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.approval_mode (5 values) ───
-- CREATE TYPE public.mcp_tool_registry_approval_mode_enum AS ENUM ('none', 'single', 'multi_step', 'risk_based', 'manual_gate');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN approval_mode TYPE public.mcp_tool_registry_approval_mode_enum
--   USING approval_mode::public.mcp_tool_registry_approval_mode_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.min_autonomy (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_min_autonomy_enum AS ENUM ('L0', 'L1', 'L2', 'L3');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN min_autonomy TYPE public.mcp_tool_registry_min_autonomy_enum
--   USING min_autonomy::public.mcp_tool_registry_min_autonomy_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.max_autonomy (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_max_autonomy_enum AS ENUM ('L0', 'L1', 'L2', 'L3');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN max_autonomy TYPE public.mcp_tool_registry_max_autonomy_enum
--   USING max_autonomy::public.mcp_tool_registry_max_autonomy_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.default_autonomy (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_default_autonomy_enum AS ENUM ('L0', 'L1', 'L2', 'L3');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN default_autonomy TYPE public.mcp_tool_registry_default_autonomy_enum
--   USING default_autonomy::public.mcp_tool_registry_default_autonomy_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';

-- ─── mcp_tool_registry.status (4 values) ───
-- CREATE TYPE public.mcp_tool_registry_status_enum AS ENUM ('draft', 'active', 'deprecated', 'retired');
-- ALTER TABLE public.mcp_tool_registry
--   ALTER COLUMN status TYPE public.mcp_tool_registry_status_enum
--   USING status::public.mcp_tool_registry_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_tool_registry'::regclass AND contype='c';
