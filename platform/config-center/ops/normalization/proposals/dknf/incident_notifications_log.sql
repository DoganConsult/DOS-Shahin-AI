-- DKNF ENUM promotions for incident_notifications_log
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── incident_notifications_log.notification_type (8 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_notifications_log_notification_type_enum AS ENUM ('escalation', 'sla_warning', 'sla_breach', 'assignment', 'status_change', 'regulatory_deadline', 'triage', 'breach_submitted');
-- ALTER TABLE __TENANT_SCHEMA__.incident_notifications_log
--   ALTER COLUMN notification_type TYPE __TENANT_SCHEMA__.incident_notifications_log_notification_type_enum
--   USING notification_type::__TENANT_SCHEMA__.incident_notifications_log_notification_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_notifications_log'::regclass AND contype='c';

-- ─── incident_notifications_log.channel (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_notifications_log_channel_enum AS ENUM ('email', 'in_app', 'sms');
-- ALTER TABLE __TENANT_SCHEMA__.incident_notifications_log
--   ALTER COLUMN channel TYPE __TENANT_SCHEMA__.incident_notifications_log_channel_enum
--   USING channel::__TENANT_SCHEMA__.incident_notifications_log_channel_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_notifications_log'::regclass AND contype='c';
