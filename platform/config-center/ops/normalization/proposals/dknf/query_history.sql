-- DKNF ENUM promotions for query_history
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── query_history.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.query_history_status_enum AS ENUM ('success', 'error', 'timeout', 'cancelled');
-- ALTER TABLE __TENANT_SCHEMA__.query_history
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.query_history_status_enum
--   USING status::__TENANT_SCHEMA__.query_history_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.query_history'::regclass AND contype='c';
