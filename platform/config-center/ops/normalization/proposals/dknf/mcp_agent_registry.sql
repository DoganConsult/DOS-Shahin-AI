-- DKNF ENUM promotions for mcp_agent_registry
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mcp_agent_registry.status (4 values) ───
-- CREATE TYPE public.mcp_agent_registry_status_enum AS ENUM ('draft', 'active', 'deprecated', 'retired');
-- ALTER TABLE public.mcp_agent_registry
--   ALTER COLUMN status TYPE public.mcp_agent_registry_status_enum
--   USING status::public.mcp_agent_registry_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_agent_registry'::regclass AND contype='c';

-- ─── mcp_agent_registry.visibility_scope (4 values) ───
-- CREATE TYPE public.mcp_agent_registry_visibility_scope_enum AS ENUM ('all', 'internal', 'admin_only', 'beta');
-- ALTER TABLE public.mcp_agent_registry
--   ALTER COLUMN visibility_scope TYPE public.mcp_agent_registry_visibility_scope_enum
--   USING visibility_scope::public.mcp_agent_registry_visibility_scope_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.mcp_agent_registry'::regclass AND contype='c';
