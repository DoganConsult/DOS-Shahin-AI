-- DKNF ENUM promotions for bcp_crisis_teams
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── bcp_crisis_teams.team_type (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.bcp_crisis_teams_team_type_enum AS ENUM ('crisis_management', 'emergency_response', 'business_recovery', 'communications', 'it_recovery');
-- ALTER TABLE __TENANT_SCHEMA__.bcp_crisis_teams
--   ALTER COLUMN team_type TYPE __TENANT_SCHEMA__.bcp_crisis_teams_team_type_enum
--   USING team_type::__TENANT_SCHEMA__.bcp_crisis_teams_team_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.bcp_crisis_teams'::regclass AND contype='c';
