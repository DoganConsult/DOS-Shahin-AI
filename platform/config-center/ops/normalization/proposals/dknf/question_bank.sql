-- DKNF ENUM promotions for question_bank
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── question_bank.question_type (10 values) ───
-- CREATE TYPE public.question_bank_question_type_enum AS ENUM ('text', 'number', 'boolean', 'date', 'select', 'multi_select', 'json', 'lookup', 'chips', 'json_rows');
-- ALTER TABLE public.question_bank
--   ALTER COLUMN question_type TYPE public.question_bank_question_type_enum
--   USING question_type::public.question_bank_question_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.question_bank'::regclass AND contype='c';
