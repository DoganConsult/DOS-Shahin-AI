-- DKNF ENUM promotions for ai_model_lifecycle
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── ai_model_lifecycle.current_state (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_model_lifecycle_current_state_enum AS ENUM ('development', 'validation', 'staging', 'production', 'deprecated', 'archived');
-- ALTER TABLE __TENANT_SCHEMA__.ai_model_lifecycle
--   ALTER COLUMN current_state TYPE __TENANT_SCHEMA__.ai_model_lifecycle_current_state_enum
--   USING current_state::__TENANT_SCHEMA__.ai_model_lifecycle_current_state_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_model_lifecycle'::regclass AND contype='c';

-- ─── ai_model_lifecycle.approval_status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_model_lifecycle_approval_status_enum AS ENUM ('pending', 'approved', 'rejected', 'not_required');
-- ALTER TABLE __TENANT_SCHEMA__.ai_model_lifecycle
--   ALTER COLUMN approval_status TYPE __TENANT_SCHEMA__.ai_model_lifecycle_approval_status_enum
--   USING approval_status::__TENANT_SCHEMA__.ai_model_lifecycle_approval_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_model_lifecycle'::regclass AND contype='c';
