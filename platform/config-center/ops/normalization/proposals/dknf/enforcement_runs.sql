-- DKNF ENUM promotions for enforcement_runs
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── enforcement_runs.verdict (3 values) ───
-- CREATE TYPE public.enforcement_runs_verdict_enum AS ENUM ('PASS', 'CONDITIONAL_PASS', 'FAIL');
-- ALTER TABLE public.enforcement_runs
--   ALTER COLUMN verdict TYPE public.enforcement_runs_verdict_enum
--   USING verdict::public.enforcement_runs_verdict_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.enforcement_runs'::regclass AND contype='c';
