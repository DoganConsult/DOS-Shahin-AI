-- DKNF ENUM promotions for crosswalk_mappings
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── crosswalk_mappings.relationship (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.crosswalk_mappings_relationship_enum AS ENUM ('equivalent', 'partial', 'related', 'derived_from');
-- ALTER TABLE __TENANT_SCHEMA__.crosswalk_mappings
--   ALTER COLUMN relationship TYPE __TENANT_SCHEMA__.crosswalk_mappings_relationship_enum
--   USING relationship::__TENANT_SCHEMA__.crosswalk_mappings_relationship_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.crosswalk_mappings'::regclass AND contype='c';
