-- DKNF ENUM promotions for agent_sod_policies
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── agent_sod_policies.proposer_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.agent_sod_policies_proposer_type_enum AS ENUM ('human', 'agent', 'service_account', 'external');
-- ALTER TABLE __TENANT_SCHEMA__.agent_sod_policies
--   ALTER COLUMN proposer_type TYPE __TENANT_SCHEMA__.agent_sod_policies_proposer_type_enum
--   USING proposer_type::__TENANT_SCHEMA__.agent_sod_policies_proposer_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.agent_sod_policies'::regclass AND contype='c';

-- ─── agent_sod_policies.approver_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.agent_sod_policies_approver_type_enum AS ENUM ('human', 'agent', 'service_account', 'external');
-- ALTER TABLE __TENANT_SCHEMA__.agent_sod_policies
--   ALTER COLUMN approver_type TYPE __TENANT_SCHEMA__.agent_sod_policies_approver_type_enum
--   USING approver_type::__TENANT_SCHEMA__.agent_sod_policies_approver_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.agent_sod_policies'::regclass AND contype='c';

-- ─── agent_sod_policies.outcome (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.agent_sod_policies_outcome_enum AS ENUM ('allow', 'warn', 'escalate', 'block');
-- ALTER TABLE __TENANT_SCHEMA__.agent_sod_policies
--   ALTER COLUMN outcome TYPE __TENANT_SCHEMA__.agent_sod_policies_outcome_enum
--   USING outcome::__TENANT_SCHEMA__.agent_sod_policies_outcome_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.agent_sod_policies'::regclass AND contype='c';
