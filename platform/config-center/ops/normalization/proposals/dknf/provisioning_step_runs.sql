-- DKNF ENUM promotions for provisioning_step_runs
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── provisioning_step_runs.status (3 values) ───
-- CREATE TYPE public.provisioning_step_runs_status_enum AS ENUM ('running', 'succeeded', 'failed');
-- ALTER TABLE public.provisioning_step_runs
--   ALTER COLUMN status TYPE public.provisioning_step_runs_status_enum
--   USING status::public.provisioning_step_runs_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.provisioning_step_runs'::regclass AND contype='c';
