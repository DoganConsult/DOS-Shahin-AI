-- DKNF ENUM promotions for risk_assessment_items
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── risk_assessment_items.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_assessment_items_status_enum AS ENUM ('pending', 'in_progress', 'submitted', 'reviewed', 'approved');
-- ALTER TABLE __TENANT_SCHEMA__.risk_assessment_items
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.risk_assessment_items_status_enum
--   USING status::__TENANT_SCHEMA__.risk_assessment_items_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_assessment_items'::regclass AND contype='c';
