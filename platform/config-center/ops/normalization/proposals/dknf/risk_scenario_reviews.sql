-- DKNF ENUM promotions for risk_scenario_reviews
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── risk_scenario_reviews.decision (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_scenario_reviews_decision_enum AS ENUM ('approved', 'rejected', 'needs_revision');
-- ALTER TABLE __TENANT_SCHEMA__.risk_scenario_reviews
--   ALTER COLUMN decision TYPE __TENANT_SCHEMA__.risk_scenario_reviews_decision_enum
--   USING decision::__TENANT_SCHEMA__.risk_scenario_reviews_decision_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_scenario_reviews'::regclass AND contype='c';
