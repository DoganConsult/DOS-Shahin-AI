-- DKNF ENUM promotions for approval_requests
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── approval_requests.status (4 values) ───
-- CREATE TYPE public.approval_requests_status_enum AS ENUM ('pending', 'approved', 'rejected', 'escalated');
-- ALTER TABLE public.approval_requests
--   ALTER COLUMN status TYPE public.approval_requests_status_enum
--   USING status::public.approval_requests_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.approval_requests'::regclass AND contype='c';
