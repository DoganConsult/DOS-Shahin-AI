-- DKNF ENUM promotions for pir_sign_offs
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── pir_sign_offs.decision (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.pir_sign_offs_decision_enum AS ENUM ('pending', 'approved', 'rejected', 'deferred');
-- ALTER TABLE __TENANT_SCHEMA__.pir_sign_offs
--   ALTER COLUMN decision TYPE __TENANT_SCHEMA__.pir_sign_offs_decision_enum
--   USING decision::__TENANT_SCHEMA__.pir_sign_offs_decision_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.pir_sign_offs'::regclass AND contype='c';
