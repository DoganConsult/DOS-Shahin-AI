-- DKNF ENUM promotions for fitch_mappings
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── fitch_mappings.risk_level (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.fitch_mappings_risk_level_enum AS ENUM ('minimal', 'low', 'moderate', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.fitch_mappings
--   ALTER COLUMN risk_level TYPE __TENANT_SCHEMA__.fitch_mappings_risk_level_enum
--   USING risk_level::__TENANT_SCHEMA__.fitch_mappings_risk_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.fitch_mappings'::regclass AND contype='c';
