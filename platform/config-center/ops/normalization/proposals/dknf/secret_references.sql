-- DKNF ENUM promotions for secret_references
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── secret_references.source (3 values) ───
-- CREATE TYPE public.secret_references_source_enum AS ENUM ('env', 'vault', 'config');
-- ALTER TABLE public.secret_references
--   ALTER COLUMN source TYPE public.secret_references_source_enum
--   USING source::public.secret_references_source_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.secret_references'::regclass AND contype='c';
