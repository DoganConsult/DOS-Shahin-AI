-- DKNF ENUM promotions for ai_risk_assessments
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── ai_risk_assessments.risk_level (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_risk_assessments_risk_level_enum AS ENUM ('minimal', 'low', 'moderate', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.ai_risk_assessments
--   ALTER COLUMN risk_level TYPE __TENANT_SCHEMA__.ai_risk_assessments_risk_level_enum
--   USING risk_level::__TENANT_SCHEMA__.ai_risk_assessments_risk_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_risk_assessments'::regclass AND contype='c';

-- ─── ai_risk_assessments.status (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_risk_assessments_status_enum AS ENUM ('draft', 'completed', 'approved');
-- ALTER TABLE __TENANT_SCHEMA__.ai_risk_assessments
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.ai_risk_assessments_status_enum
--   USING status::__TENANT_SCHEMA__.ai_risk_assessments_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_risk_assessments'::regclass AND contype='c';
