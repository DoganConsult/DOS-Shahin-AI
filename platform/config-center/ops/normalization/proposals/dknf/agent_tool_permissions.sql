-- DKNF ENUM promotions for agent_tool_permissions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── agent_tool_permissions.level (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.agent_tool_permissions_level_enum AS ENUM ('allow', 'deny', 'approval_required');
-- ALTER TABLE __TENANT_SCHEMA__.agent_tool_permissions
--   ALTER COLUMN level TYPE __TENANT_SCHEMA__.agent_tool_permissions_level_enum
--   USING level::__TENANT_SCHEMA__.agent_tool_permissions_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.agent_tool_permissions'::regclass AND contype='c';
