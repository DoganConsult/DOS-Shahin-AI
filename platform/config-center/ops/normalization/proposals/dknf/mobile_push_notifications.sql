-- DKNF ENUM promotions for mobile_push_notifications
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mobile_push_notifications.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.mobile_push_notifications_status_enum AS ENUM ('pending', 'sent', 'delivered', 'failed', 'read');
-- ALTER TABLE __TENANT_SCHEMA__.mobile_push_notifications
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.mobile_push_notifications_status_enum
--   USING status::__TENANT_SCHEMA__.mobile_push_notifications_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.mobile_push_notifications'::regclass AND contype='c';
