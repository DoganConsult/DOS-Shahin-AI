-- DKNF ENUM promotions for mcp_sessions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mcp_sessions.actor_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.mcp_sessions_actor_type_enum AS ENUM ('human', 'agent', 'service_account', 'external');
-- ALTER TABLE __TENANT_SCHEMA__.mcp_sessions
--   ALTER COLUMN actor_type TYPE __TENANT_SCHEMA__.mcp_sessions_actor_type_enum
--   USING actor_type::__TENANT_SCHEMA__.mcp_sessions_actor_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.mcp_sessions'::regclass AND contype='c';

-- ─── mcp_sessions.status (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.mcp_sessions_status_enum AS ENUM ('open', 'closed', 'errored');
-- ALTER TABLE __TENANT_SCHEMA__.mcp_sessions
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.mcp_sessions_status_enum
--   USING status::__TENANT_SCHEMA__.mcp_sessions_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.mcp_sessions'::regclass AND contype='c';
