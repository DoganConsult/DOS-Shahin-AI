-- DKNF ENUM promotions for lookup_siem_providers
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_siem_providers.deployment_type (3 values) ───
-- CREATE TYPE public.lookup_siem_providers_deployment_type_enum AS ENUM ('cloud', 'on-prem', 'hybrid');
-- ALTER TABLE public.lookup_siem_providers
--   ALTER COLUMN deployment_type TYPE public.lookup_siem_providers_deployment_type_enum
--   USING deployment_type::public.lookup_siem_providers_deployment_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_siem_providers'::regclass AND contype='c';
