-- DKNF ENUM promotions for exception_requests
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── exception_requests.status (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.exception_requests_status_enum AS ENUM ('pending', 'under_review', 'approved', 'rejected', 'expired', 'revoked');
-- ALTER TABLE __TENANT_SCHEMA__.exception_requests
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.exception_requests_status_enum
--   USING status::__TENANT_SCHEMA__.exception_requests_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.exception_requests'::regclass AND contype='c';
