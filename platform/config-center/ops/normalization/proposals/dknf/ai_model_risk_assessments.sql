-- DKNF ENUM promotions for ai_model_risk_assessments
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── ai_model_risk_assessments.assessment_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_model_risk_assessments_assessment_type_enum AS ENUM ('automated', 'manual', 'hybrid', 'external_audit');
-- ALTER TABLE __TENANT_SCHEMA__.ai_model_risk_assessments
--   ALTER COLUMN assessment_type TYPE __TENANT_SCHEMA__.ai_model_risk_assessments_assessment_type_enum
--   USING assessment_type::__TENANT_SCHEMA__.ai_model_risk_assessments_assessment_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_model_risk_assessments'::regclass AND contype='c';

-- ─── ai_model_risk_assessments.risk_level (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_model_risk_assessments_risk_level_enum AS ENUM ('low', 'medium', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.ai_model_risk_assessments
--   ALTER COLUMN risk_level TYPE __TENANT_SCHEMA__.ai_model_risk_assessments_risk_level_enum
--   USING risk_level::__TENANT_SCHEMA__.ai_model_risk_assessments_risk_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_model_risk_assessments'::regclass AND contype='c';
