-- DKNF ENUM promotions for sso_sessions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── sso_sessions.protocol (2 values) ───
-- CREATE TYPE public.sso_sessions_protocol_enum AS ENUM ('saml', 'oidc');
-- ALTER TABLE public.sso_sessions
--   ALTER COLUMN protocol TYPE public.sso_sessions_protocol_enum
--   USING protocol::public.sso_sessions_protocol_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.sso_sessions'::regclass AND contype='c';
