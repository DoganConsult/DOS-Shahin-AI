-- DKNF ENUM promotions for training_assignments
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── training_assignments.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.training_assignments_status_enum AS ENUM ('assigned', 'in_progress', 'completed', 'overdue', 'waived');
-- ALTER TABLE __TENANT_SCHEMA__.training_assignments
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.training_assignments_status_enum
--   USING status::__TENANT_SCHEMA__.training_assignments_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.training_assignments'::regclass AND contype='c';
