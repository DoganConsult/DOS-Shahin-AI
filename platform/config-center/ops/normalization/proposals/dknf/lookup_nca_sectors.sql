-- DKNF ENUM promotions for lookup_nca_sectors
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_nca_sectors.criticality_level (3 values) ───
-- CREATE TYPE public.lookup_nca_sectors_criticality_level_enum AS ENUM ('critical', 'important', 'standard');
-- ALTER TABLE public.lookup_nca_sectors
--   ALTER COLUMN criticality_level TYPE public.lookup_nca_sectors_criticality_level_enum
--   USING criticality_level::public.lookup_nca_sectors_criticality_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_nca_sectors'::regclass AND contype='c';
