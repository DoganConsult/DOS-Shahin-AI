-- DKNF ENUM promotions for platform_outbox
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── platform_outbox.status (3 values) ───
-- CREATE TYPE public.platform_outbox_status_enum AS ENUM ('pending', 'published', 'dead');
-- ALTER TABLE public.platform_outbox
--   ALTER COLUMN status TYPE public.platform_outbox_status_enum
--   USING status::public.platform_outbox_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.platform_outbox'::regclass AND contype='c';
