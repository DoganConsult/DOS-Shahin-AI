-- DKNF ENUM promotions for regulatory_mappings
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── regulatory_mappings.compliance_status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.regulatory_mappings_compliance_status_enum AS ENUM ('compliant', 'partial', 'non_compliant', 'unknown', 'na');
-- ALTER TABLE __TENANT_SCHEMA__.regulatory_mappings
--   ALTER COLUMN compliance_status TYPE __TENANT_SCHEMA__.regulatory_mappings_compliance_status_enum
--   USING compliance_status::__TENANT_SCHEMA__.regulatory_mappings_compliance_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.regulatory_mappings'::regclass AND contype='c';
