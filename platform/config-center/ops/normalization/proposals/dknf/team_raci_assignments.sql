-- DKNF ENUM promotions for team_raci_assignments
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── team_raci_assignments.raci_role (4 values) ───
-- CREATE TYPE public.team_raci_assignments_raci_role_enum AS ENUM ('responsible', 'accountable', 'consulted', 'informed');
-- ALTER TABLE public.team_raci_assignments
--   ALTER COLUMN raci_role TYPE public.team_raci_assignments_raci_role_enum
--   USING raci_role::public.team_raci_assignments_raci_role_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.team_raci_assignments'::regclass AND contype='c';
