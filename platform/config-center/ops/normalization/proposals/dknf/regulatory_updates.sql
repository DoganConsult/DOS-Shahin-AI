-- DKNF ENUM promotions for regulatory_updates
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── regulatory_updates.impact_level (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.regulatory_updates_impact_level_enum AS ENUM ('low', 'medium', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.regulatory_updates
--   ALTER COLUMN impact_level TYPE __TENANT_SCHEMA__.regulatory_updates_impact_level_enum
--   USING impact_level::__TENANT_SCHEMA__.regulatory_updates_impact_level_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.regulatory_updates'::regclass AND contype='c';

-- ─── regulatory_updates.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.regulatory_updates_status_enum AS ENUM ('new', 'under_review', 'actioned', 'archived');
-- ALTER TABLE __TENANT_SCHEMA__.regulatory_updates
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.regulatory_updates_status_enum
--   USING status::__TENANT_SCHEMA__.regulatory_updates_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.regulatory_updates'::regclass AND contype='c';
