-- DKNF ENUM promotions for mobile_device_registrations
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── mobile_device_registrations.platform (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.mobile_device_registrations_platform_enum AS ENUM ('ios', 'android', 'web');
-- ALTER TABLE __TENANT_SCHEMA__.mobile_device_registrations
--   ALTER COLUMN platform TYPE __TENANT_SCHEMA__.mobile_device_registrations_platform_enum
--   USING platform::__TENANT_SCHEMA__.mobile_device_registrations_platform_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.mobile_device_registrations'::regclass AND contype='c';
