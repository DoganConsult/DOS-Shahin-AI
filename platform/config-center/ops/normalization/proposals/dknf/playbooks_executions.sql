-- DKNF ENUM promotions for playbooks_executions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── playbooks_executions.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.playbooks_executions_status_enum AS ENUM ('running', 'completed', 'failed', 'cancelled', 'paused');
-- ALTER TABLE __TENANT_SCHEMA__.playbooks_executions
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.playbooks_executions_status_enum
--   USING status::__TENANT_SCHEMA__.playbooks_executions_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.playbooks_executions'::regclass AND contype='c';
