-- DKNF ENUM promotions for playbooks_ai_suggestions
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── playbooks_ai_suggestions.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.playbooks_ai_suggestions_status_enum AS ENUM ('pending', 'accepted', 'rejected', 'expired');
-- ALTER TABLE __TENANT_SCHEMA__.playbooks_ai_suggestions
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.playbooks_ai_suggestions_status_enum
--   USING status::__TENANT_SCHEMA__.playbooks_ai_suggestions_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.playbooks_ai_suggestions'::regclass AND contype='c';
