-- DKNF ENUM promotions for control_tests
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── control_tests.status (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.control_tests_status_enum AS ENUM ('planned', 'in_progress', 'completed', 'failed');
-- ALTER TABLE __TENANT_SCHEMA__.control_tests
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.control_tests_status_enum
--   USING status::__TENANT_SCHEMA__.control_tests_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.control_tests'::regclass AND contype='c';

-- ─── control_tests.result (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.control_tests_result_enum AS ENUM ('pass', 'fail', 'exception');
-- ALTER TABLE __TENANT_SCHEMA__.control_tests
--   ALTER COLUMN result TYPE __TENANT_SCHEMA__.control_tests_result_enum
--   USING result::__TENANT_SCHEMA__.control_tests_result_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.control_tests'::regclass AND contype='c';
