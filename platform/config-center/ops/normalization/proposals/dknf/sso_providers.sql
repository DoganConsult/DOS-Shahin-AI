-- DKNF ENUM promotions for sso_providers
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── sso_providers.protocol (2 values) ───
-- CREATE TYPE public.sso_providers_protocol_enum AS ENUM ('saml', 'oidc');
-- ALTER TABLE public.sso_providers
--   ALTER COLUMN protocol TYPE public.sso_providers_protocol_enum
--   USING protocol::public.sso_providers_protocol_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.sso_providers'::regclass AND contype='c';

-- ─── sso_providers.status (3 values) ───
-- CREATE TYPE public.sso_providers_status_enum AS ENUM ('active', 'inactive', 'testing');
-- ALTER TABLE public.sso_providers
--   ALTER COLUMN status TYPE public.sso_providers_status_enum
--   USING status::public.sso_providers_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.sso_providers'::regclass AND contype='c';
