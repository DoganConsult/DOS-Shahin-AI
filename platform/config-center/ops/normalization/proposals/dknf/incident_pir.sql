-- DKNF ENUM promotions for incident_pir
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── incident_pir.pir_type (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_pir_pir_type_enum AS ENUM ('standard', 'major', 'regulatory', 'executive');
-- ALTER TABLE __TENANT_SCHEMA__.incident_pir
--   ALTER COLUMN pir_type TYPE __TENANT_SCHEMA__.incident_pir_pir_type_enum
--   USING pir_type::__TENANT_SCHEMA__.incident_pir_pir_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_pir'::regclass AND contype='c';

-- ─── incident_pir.status (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_pir_status_enum AS ENUM ('draft', 'in_progress', 'review', 'sign_off', 'completed', 'archived');
-- ALTER TABLE __TENANT_SCHEMA__.incident_pir
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.incident_pir_status_enum
--   USING status::__TENANT_SCHEMA__.incident_pir_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_pir'::regclass AND contype='c';

-- ─── incident_pir.effectiveness_status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.incident_pir_effectiveness_status_enum AS ENUM ('pending', 'effective', 'partially_effective', 'ineffective', 'not_reviewed');
-- ALTER TABLE __TENANT_SCHEMA__.incident_pir
--   ALTER COLUMN effectiveness_status TYPE __TENANT_SCHEMA__.incident_pir_effectiveness_status_enum
--   USING effectiveness_status::__TENANT_SCHEMA__.incident_pir_effectiveness_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.incident_pir'::regclass AND contype='c';
