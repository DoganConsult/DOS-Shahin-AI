-- DKNF ENUM promotions for risk_reviews
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── risk_reviews.review_type (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_reviews_review_type_enum AS ENUM ('periodic', 'escalation', 'appetite_breach', 'treatment_completion', 'closure');
-- ALTER TABLE __TENANT_SCHEMA__.risk_reviews
--   ALTER COLUMN review_type TYPE __TENANT_SCHEMA__.risk_reviews_review_type_enum
--   USING review_type::__TENANT_SCHEMA__.risk_reviews_review_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_reviews'::regclass AND contype='c';

-- ─── risk_reviews.priority (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_reviews_priority_enum AS ENUM ('low', 'medium', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.risk_reviews
--   ALTER COLUMN priority TYPE __TENANT_SCHEMA__.risk_reviews_priority_enum
--   USING priority::__TENANT_SCHEMA__.risk_reviews_priority_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_reviews'::regclass AND contype='c';

-- ─── risk_reviews.status (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.risk_reviews_status_enum AS ENUM ('pending', 'in_review', 'approved', 'rejected', 'escalated', 'completed');
-- ALTER TABLE __TENANT_SCHEMA__.risk_reviews
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.risk_reviews_status_enum
--   USING status::__TENANT_SCHEMA__.risk_reviews_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.risk_reviews'::regclass AND contype='c';
