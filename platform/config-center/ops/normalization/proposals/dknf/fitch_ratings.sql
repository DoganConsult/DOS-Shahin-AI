-- DKNF ENUM promotions for fitch_ratings
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── fitch_ratings.outlook (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.fitch_ratings_outlook_enum AS ENUM ('stable', 'positive', 'negative', 'watch');
-- ALTER TABLE __TENANT_SCHEMA__.fitch_ratings
--   ALTER COLUMN outlook TYPE __TENANT_SCHEMA__.fitch_ratings_outlook_enum
--   USING outlook::__TENANT_SCHEMA__.fitch_ratings_outlook_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.fitch_ratings'::regclass AND contype='c';
