-- DKNF ENUM promotions for conditional_access_policies
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── conditional_access_policies.action (3 values) ───
-- CREATE TYPE public.conditional_access_policies_action_enum AS ENUM ('allow', 'deny', 'require_mfa');
-- ALTER TABLE public.conditional_access_policies
--   ALTER COLUMN action TYPE public.conditional_access_policies_action_enum
--   USING action::public.conditional_access_policies_action_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.conditional_access_policies'::regclass AND contype='c';
