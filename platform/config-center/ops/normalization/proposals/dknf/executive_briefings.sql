-- DKNF ENUM promotions for executive_briefings
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── executive_briefings.status (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.executive_briefings_status_enum AS ENUM ('draft', 'approved', 'distributed');
-- ALTER TABLE __TENANT_SCHEMA__.executive_briefings
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.executive_briefings_status_enum
--   USING status::__TENANT_SCHEMA__.executive_briefings_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.executive_briefings'::regclass AND contype='c';
