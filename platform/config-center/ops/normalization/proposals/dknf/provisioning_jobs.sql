-- DKNF ENUM promotions for provisioning_jobs
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── provisioning_jobs.job_status (4 values) ───
-- CREATE TYPE public.provisioning_jobs_job_status_enum AS ENUM ('queued', 'running', 'completed', 'failed');
-- ALTER TABLE public.provisioning_jobs
--   ALTER COLUMN job_status TYPE public.provisioning_jobs_job_status_enum
--   USING job_status::public.provisioning_jobs_job_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.provisioning_jobs'::regclass AND contype='c';
