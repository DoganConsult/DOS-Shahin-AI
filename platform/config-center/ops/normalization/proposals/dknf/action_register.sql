-- DKNF ENUM promotions for action_register
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── action_register.status (5 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.action_register_status_enum AS ENUM ('open', 'in_progress', 'completed', 'overdue', 'cancelled');
-- ALTER TABLE __TENANT_SCHEMA__.action_register
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.action_register_status_enum
--   USING status::__TENANT_SCHEMA__.action_register_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.action_register'::regclass AND contype='c';
