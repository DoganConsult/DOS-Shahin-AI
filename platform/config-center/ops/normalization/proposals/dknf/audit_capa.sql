-- DKNF ENUM promotions for audit_capa
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── audit_capa.capa_type (2 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.audit_capa_capa_type_enum AS ENUM ('corrective', 'preventive');
-- ALTER TABLE __TENANT_SCHEMA__.audit_capa
--   ALTER COLUMN capa_type TYPE __TENANT_SCHEMA__.audit_capa_capa_type_enum
--   USING capa_type::__TENANT_SCHEMA__.audit_capa_capa_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.audit_capa'::regclass AND contype='c';
