-- DKNF ENUM promotions for lookup_ncec_sectors
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_ncec_sectors.criticality (3 values) ───
-- CREATE TYPE public.lookup_ncec_sectors_criticality_enum AS ENUM ('critical', 'important', 'standard');
-- ALTER TABLE public.lookup_ncec_sectors
--   ALTER COLUMN criticality TYPE public.lookup_ncec_sectors_criticality_enum
--   USING criticality::public.lookup_ncec_sectors_criticality_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_ncec_sectors'::regclass AND contype='c';
