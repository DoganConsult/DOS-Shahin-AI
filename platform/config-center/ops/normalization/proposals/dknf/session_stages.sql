-- DKNF ENUM promotions for session_stages
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── session_stages.status (5 values) ───
-- CREATE TYPE public.session_stages_status_enum AS ENUM ('not_started', 'in_progress', 'completed', 'skipped', 'blocked');
-- ALTER TABLE public.session_stages
--   ALTER COLUMN status TYPE public.session_stages_status_enum
--   USING status::public.session_stages_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.session_stages'::regclass AND contype='c';
