-- DKNF ENUM promotions for ai_activity_alerts
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── ai_activity_alerts.severity (3 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.ai_activity_alerts_severity_enum AS ENUM ('info', 'warning', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.ai_activity_alerts
--   ALTER COLUMN severity TYPE __TENANT_SCHEMA__.ai_activity_alerts_severity_enum
--   USING severity::__TENANT_SCHEMA__.ai_activity_alerts_severity_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.ai_activity_alerts'::regclass AND contype='c';
