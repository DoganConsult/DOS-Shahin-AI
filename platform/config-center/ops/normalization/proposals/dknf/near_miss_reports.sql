-- DKNF ENUM promotions for near_miss_reports
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── near_miss_reports.severity_estimate (4 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.near_miss_reports_severity_estimate_enum AS ENUM ('low', 'medium', 'high', 'critical');
-- ALTER TABLE __TENANT_SCHEMA__.near_miss_reports
--   ALTER COLUMN severity_estimate TYPE __TENANT_SCHEMA__.near_miss_reports_severity_estimate_enum
--   USING severity_estimate::__TENANT_SCHEMA__.near_miss_reports_severity_estimate_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.near_miss_reports'::regclass AND contype='c';

-- ─── near_miss_reports.status (6 values) ───
-- CREATE TYPE __TENANT_SCHEMA__.near_miss_reports_status_enum AS ENUM ('reported', 'under_review', 'action_taken', 'converted', 'closed', 'dismissed');
-- ALTER TABLE __TENANT_SCHEMA__.near_miss_reports
--   ALTER COLUMN status TYPE __TENANT_SCHEMA__.near_miss_reports_status_enum
--   USING status::__TENANT_SCHEMA__.near_miss_reports_status_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = '__TENANT_SCHEMA__.near_miss_reports'::regclass AND contype='c';
