-- DKNF ENUM promotions for lookup_cloud_providers
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_cloud_providers.provider_type (4 values) ───
-- CREATE TYPE public.lookup_cloud_providers_provider_type_enum AS ENUM ('public', 'private', 'hybrid', 'sovereign');
-- ALTER TABLE public.lookup_cloud_providers
--   ALTER COLUMN provider_type TYPE public.lookup_cloud_providers_provider_type_enum
--   USING provider_type::public.lookup_cloud_providers_provider_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_cloud_providers'::regclass AND contype='c';
