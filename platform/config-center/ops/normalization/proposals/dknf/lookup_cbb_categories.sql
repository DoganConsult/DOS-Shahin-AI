-- DKNF ENUM promotions for lookup_cbb_categories
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_cbb_categories.category_type (2 values) ───
-- CREATE TYPE public.lookup_cbb_categories_category_type_enum AS ENUM ('conventional', 'islamic');
-- ALTER TABLE public.lookup_cbb_categories
--   ALTER COLUMN category_type TYPE public.lookup_cbb_categories_category_type_enum
--   USING category_type::public.lookup_cbb_categories_category_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_cbb_categories'::regclass AND contype='c';
