-- DKNF ENUM promotions for lookup_cbuae_categories
-- Generated by dknf-fix-plan.ts. All statements commented out.
-- Per column: create a dedicated ENUM type, then ALTER the column
-- TYPE to use it (implicit cast via USING), then drop the CHECK constraint.

-- ─── lookup_cbuae_categories.entity_type (5 values) ───
-- CREATE TYPE public.lookup_cbuae_categories_entity_type_enum AS ENUM ('bank', 'insurance', 'broker', 'exchange', 'payment');
-- ALTER TABLE public.lookup_cbuae_categories
--   ALTER COLUMN entity_type TYPE public.lookup_cbuae_categories_entity_type_enum
--   USING entity_type::public.lookup_cbuae_categories_entity_type_enum;
-- -- drop the now-redundant CHECK constraint; name is Postgres-auto unless you renamed it:
-- -- SELECT conname FROM pg_constraint WHERE conrelid = 'public.lookup_cbuae_categories'::regclass AND contype='c';
