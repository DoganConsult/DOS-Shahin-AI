# Ownership manifest for modules/reporting
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: reporting
version: 1
migrationsDir:
  tenant: modules/reporting/db/tenant/migrations
  public: modules/reporting/db/public/migrations
ownedTables:
  tenant:
    prefix: [reporting, report]
    # 30 tenant tables owned (derived from catalog)
    count: 30
  public:
    # 1 public tables owned
    count: 1
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 4
