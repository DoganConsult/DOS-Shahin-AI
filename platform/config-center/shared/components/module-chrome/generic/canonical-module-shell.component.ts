// Shell-only build stub: original re-export pulled in deeply broken
// PrimeNG v19 chrome components. Replaced with a minimal Angular stub
// preserving the public class symbol consumed by ShellHostComponent.
import { Component, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'app-canonical-module-shell',
  standalone: true,
  changeDetection: ChangeDetectionStrategy.OnPush,
  template: '',
})
export class CanonicalModuleShellComponent {}
