# Canonical operator stub — referenced by platform/config-center/ops/ports.allocation.json (external.vault).
# HashiCorp Vault API / cluster RPC; values mirror allocation note.
HOST=127.0.0.1
PORT=8200
CLUSTER_PORT=8201
