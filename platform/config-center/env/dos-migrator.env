# DOS Platform — migration runner (one-shot).
# Runs as the dos_migrator role, which owns dos.platform_migrations and the
# dos / platform_dauth / public schemas. Runtime services use dos_auth and
# do NOT have permission to mutate the migrations ledger.
NODE_ENV=production
LOG_LEVEL=info
DATABASE_URL=postgresql://dos_migrator:dos_migrator_pass_2026@localhost:5432/shahin_grc
