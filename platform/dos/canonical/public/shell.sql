-- ═══════════════════════════════════════════════════════════════════
-- CANONICAL SQL — module=dos scope=public function=shell
-- ledger: dos.schema_migrations
-- Generated by ops/scripts/generate-canonical-sql.mjs from immutable history.
-- This file is the consolidated CREATE TABLE/INDEX/VIEW shape for the function.
-- Future schema changes MUST be applied as numbered delta migrations under
-- platform/dos/migrations/public/ and back-merged here when applied. Do not edit by hand.
--
-- Tables covered (1):
--   * shell_config
--
-- Source migrations (immutable, 1):
--   * platform/dos/migrations/public/20260425_0006_shell_config_store.sql
-- ═══════════════════════════════════════════════════════════════════

CREATE TABLE IF NOT EXISTS dos.shell_config (
  config_id     UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id     VARCHAR(64) NOT NULL,
  scope_type    VARCHAR(20) NOT NULL
                CHECK (scope_type IN ('platform', 'tenant', 'role', 'user')),
  scope_key     VARCHAR(200),
  module_code   VARCHAR(100) NOT NULL,
  product_code  VARCHAR(100),
  config        JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  updated_by    VARCHAR(64)
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_dos_shell_config_scope
  ON dos.shell_config (tenant_id, scope_type, COALESCE(scope_key, ''), module_code, COALESCE(product_code, ''));

CREATE INDEX IF NOT EXISTS ix_dos_shell_config_tenant_module
  ON dos.shell_config (tenant_id, module_code, product_code);
