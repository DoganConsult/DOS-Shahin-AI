-- dos:extracted-from: ops/migrations/tenant/099_bootstrap_tenant_domain_tables.sql
-- dos:supersedes-checksum: a61d4fa7d87b2bfaa14ca1d8b5621fb5e3dd11b0b657c87c44444acdaf044140
-- dos:owning-module: ai
-- dos:tables: ai_model_registry
-- dos:statement-count: 4
-- Generated by ops/normalization/scripts/split-megafile.ts
--
-- The migration runner treats this file as already applied whenever
-- the parent file's checksum is recorded in dos.platform_migrations.
-- Fresh databases re-run the parent first, then skip this shard.
-- ── ai_model_registry — FK target for 111 + governance-ai module ──────────
-- 111 FKs to ai_model_registry(model_version_id); governance-ai module
-- FKs to ai_model_registry(id). Provide both as UNIQUE.
CREATE TABLE IF NOT EXISTS __TENANT_SCHEMA__.ai_model_registry (
  id                UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  model_version_id  UUID NOT NULL UNIQUE DEFAULT gen_random_uuid(),
  tenant_id         UUID,
  model_name        VARCHAR(255),
  version           VARCHAR(50),
  created_at        TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

ALTER TABLE __TENANT_SCHEMA__.ai_model_registry ADD COLUMN IF NOT EXISTS id UUID DEFAULT gen_random_uuid();

ALTER TABLE __TENANT_SCHEMA__.ai_model_registry ADD COLUMN IF NOT EXISTS model_version_id UUID DEFAULT gen_random_uuid();

CREATE INDEX IF NOT EXISTS idx_ai_model_registry_tenant_bootstrap ON __TENANT_SCHEMA__.ai_model_registry (tenant_id);
