import { z } from 'zod';

export const grcJsonMetadata = z.object({});
export const grcPositiveInt = z.object({});
export const paginationQuery = z.object({});
export const bulkIdsBody = z.object({});
export const statusFilter = z.object({});
