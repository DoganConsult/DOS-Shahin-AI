/**
 * ai — Lifecycle Registry Registration
 * Auto-generated by scripts/codegen/generate-lifecycle-registrations.ts
 *
 * Registers entity lifecycles with the central LifecycleRegistry (DOS-owned).
 * DAuth evaluateLifecycleTransition() consumes these definitions.
 */

import { registerLifecycleDefinition } from './ports/lifecycle.port';

const AI_AGENTS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'decommissioned',
  'archived',
] as const;

const AI_AGENTS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'decommissioned'],
  suspended       : ['active', 'decommissioned', 'archived'],
  decommissioned  : ['archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'ai',
  'ai_agents',
  AI_AGENTS_STATES,
  AI_AGENTS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const AI_AGENT_CONFIGS_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const AI_AGENT_CONFIGS_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'ai',
  'ai_agent_configs',
  AI_AGENT_CONFIGS_STATES,
  AI_AGENT_CONFIGS_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);

const AI_EXECUTION_LOG_STATES = [
  'draft',
  'in_review',
  'approved',
  'active',
  'suspended',
  'archived',
] as const;

const AI_EXECUTION_LOG_TRANSITIONS: Record<string, string[]> = {
  draft           : ['in_review'],
  in_review       : ['approved', 'draft'],
  approved        : ['active'],
  active          : ['suspended', 'archived'],
  suspended       : ['active', 'archived'],
  archived        : [],
};

registerLifecycleDefinition(
  'ai',
  'ai_execution_log',
  AI_EXECUTION_LOG_STATES,
  AI_EXECUTION_LOG_TRANSITIONS,
  {
    initialState: 'draft',
    terminalStates: ['archived'],
  },
);
