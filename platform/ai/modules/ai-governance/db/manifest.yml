# Ownership manifest for modules/ai-governance
# Generated by ops/normalization/scripts/emit-manifests.ts
# Source of truth: ops/normalization/module-prefixes.yml

module: ai-governance
version: 1
migrationsDir:
  tenant: modules/ai-governance/db/tenant/migrations
  public: modules/ai-governance/db/public/migrations
ownedTables:
  tenant:
    prefix: [agent, copilot]
    explicit_prefix: [ai_governance, governance_ai, agent_sod, agent_tool, agent_credentials]
    # 69 tenant tables owned (derived from catalog)
    count: 69
  public:
    # 10 public tables owned
    count: 10
  explicit: [ai_model_risk_register_link, autonomous_engine_log]
migrations:
  # Files currently classified to this module (pre-git-mv paths):
  file_count: 8
